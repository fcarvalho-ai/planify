# État du projet — Planning Post Prod

Version : `0.5.0-rc6`
Date : 2026-08-25

Organisation : gouvernance multi-agents formalisée dans `AGENTS.md`; dépôt Git initialisé sur `main`.

Architecture cible : le synoptique global fourni par le Product Owner est adopté comme cible de développement. La migration reste incrémentale depuis la RC1, sans big bang.

Gates indépendants post-RC6 — comparaison mensuelle 2026-08-26 : **BLOQUÉ — retour DEV requis** sur le candidat exact `7b723b3ce6c43c9fb5ccc0ab9f016c2430429629`. QA est `APPROVED` (0 P0/P1) et PERFORMANCE est `APPROVED` (p95 `16,68 ms` sur 250 ressources/10 000 réservations/2 000 documents), mais REVIEW est `REJECTED` avec 2 P1 et SECURITY `REJECTED` avec 1 P1. Blocages : (1) `asOf` ne borne pas encore une acceptation ou conversion postérieure dans le mois courant ; (2) `/dashboard/overview` n’exige pas explicitement `dashboard.read` et l’UI fabrique un faux delta `0 €` sans `quote.read` ; (3) un Devis accepté puis remplacé disparaît rétroactivement du CA signé du mois historique malgré son `acceptedAt`. Preuves communes : ciblés jusqu’à `108/108`, suite complète `354/354`, lint/build/diff-check PASS. Condition de déblocage : corriger ces trois chemins et leurs négatifs, puis rejouer REVIEW, QA et SECURITY/PERFORMANCE impactés sur un nouveau candidat exact avant Integration/E2E.

Retour DEV post-RC6 — comparaison mensuelle de la Vue d’ensemble 2026-08-25 : **IMPLÉMENTÉ — gates indépendants requis**. Un bloc compact placé sous la synthèse permet de choisir l’un des 24 mois civils passés et le compare au mois courant. Les quatre lectures sont alignées : occupation globale, CA devisé, CA signé et Budget non converti, avec valeurs des deux mois et écart. Le mois précédent est proposé par défaut ; les mois courants, futurs ou mal formés répondent `422`. Le serveur recalcule chaque mois avec les mêmes scopes Société/Site/Projet/Ressource/Devis, sans transformer les montants en cumul historique ni divulguer la synthèse commerciale sans `quote.read`. OpenAPI et la spécification MVP documentent `comparisonMonth` et la réponse structurée. Preuves DEV fraîches : Dashboards `20/20`, ciblés Fondations+Dashboards+Devis `86/86`, suite complète `354/354`, lint/build/diff-check PASS ; benchmark représentatif 250 ressources/10 000 réservations, p95 `22,47 ms`. Recette navigateur : juillet 2026 sélectionné par défaut, passage à juin sans navigation, quatre comparatifs mis à jour et structure accessible. Aucun changement de persistance, migration ou donnée de démonstration. REVIEW, QA et SECURITY/PERFORMANCE indépendants restent requis avant intégration/release.

Retour DEV post-RC6 — refonte Vue d’ensemble 2026-08-25 : **IMPLÉMENTÉ — gates indépendants requis**. La page `#dashboard` présente désormais une lecture progressive et réconciliée : occupation globale sélectionnable à la journée, à la semaine et au mois ; détail à la demande des salles de montage, mixage et étalonnage ; clic salle vers le Planning avec période et ressource conservées ; tendance permanente sur les six derniers mois civils. Le volet commercial sépare le CA devisé, le CA signé et les Budgets non convertis, et reste indisponible sans `quote.read`. Le read-model serveur `DASHBOARD_OVERVIEW@1` applique Société/Site/Projet/Ressource/Devis avant agrégation, canonise les doubles options, respecte Europe/Paris et expose un contrat OpenAPI versionné. L’interface borne le détail de nombreuses salles dans un panneau local pour conserver une page simple. Preuves DEV fraîches : Dashboards `18/18`, suite complète `352/352`, lint/build/diff-check PASS ; benchmark in-memory 100 ressources/10 000 réservations p95 `36,23 ms`. Recette navigateur : Jour/Semaine/Mois, détail Montage borné à `360 px` (`2 615 px` internes) et passage vers le Planning filtré sur `resource_3` conformes. Aucun changement de persistance ni de données de démonstration. REVIEW, QA et SECURITY/PERFORMANCE doivent être rejoués avant intégration/release.

Retour DEV post-RC6 — ouverture Planning au jour courant 2026-08-25 : **IMPLÉMENTÉ — re-gates requis**. L’ancre historique codée en dur au `17/08/2026` est supprimée. Toute ouverture générale du Planning (connexion/reconnexion, navigation principale, Projet ou démarrage de planification depuis un Devis) recalcule la date civile courante en `Europe/Paris`, réinitialise les deux axes virtuels et recharge la fenêtre serveur correspondante. Les ouvertures contextuelles vers une réservation existante ou une conversion nouvellement créée conservent volontairement la date de l’élément. Le bouton Aujourd’hui utilise la même règle civile. Le badge visuel redondant « Destination » est retiré ; le contour violet de la cellule cible et le bouton `+` restent présents. Preuves DEV fraîches : Planning `49/49`, suite complète `348/348`, lint/build/diff-check PASS. Recette navigateur : ancre forcée au `17/08/2026`, sortie vers Projets, puis « Ouvrir le planning » revient au `25/08/2026`; connexion directe au Planning affiche également le `25/08/2026` et aucun badge Destination. La release `0.5.0-rc6` reste la base stable ; re-REVIEW et gates aval impactés sont requis avant intégration.

Retour DEV post-RC6 — déplacement horizontal 2026-08-25 : **IMPLÉMENTÉ — re-gates requis**. Le déplacement précis d’une cellule Planning couvre désormais les deux axes : autre date, autre salle, ou les deux, pour les réservations `option` et `confirmed`. L’identité source reste stable, les autres cellules ne bougent pas et Annuler/Rétablir mémorise la date ainsi que la salle effectives, y compris après plusieurs déplacements successifs. Le serveur refuse les dates invalides ou éloignées de plus de 370 jours, recalcule conflits/audit/SSE et restitue une cellule déplacée hors de la période de base dans les requêtes de fenêtre et de ressource cibles. OpenAPI et la spécification MVP sont alignés. Preuves DEV fraîches : Planning `47/47`, API `42/42`, suite complète `346/346`, lint/build/diff-check PASS. Recette navigateur Northlight Post : cellule confirmée `reservation_1` avancée `17→18 août` dans AVID 103, Annuler puis Rétablir conformes, restauration finale ; recul `17→16 août` puis restauration conformes. La base de démonstration est revenue à son état initial.

Retour DEV post-RC6 2026-08-25 : **IMPLÉMENTÉ — re-gates requis**. La sélection Planning ne transforme plus une cellule en déplacement de la réservation entière : une cellule unique peut être déplacée vers une autre salle le même jour, pour les statuts `option` et `confirmed`, via l’endpoint transactionnel existant, avec présence courte, contrôle de version, audit/SSE et compensation Annuler/Rétablir. Le déplacement conserve les autres jours et retire effectivement la cellule de sa salle d’origine. Le chemin serveur accepte aussi les réservations historiques dépourvues de politique temporelle explicite, sans promouvoir silencieusement leurs champs persistés. La barre de sélection est compactée : actions indisponibles masquées, libellés courts, aide en infobulle et date localisée. Preuves DEV fraîches : Planning `47/47`, API `42/42`, suite complète `346/346`, lint/build/diff-check PASS. Recette navigateur sur la démonstration Northlight Post : `reservation_1` confirmée déplacée seule le 17 août d’AVID 103 vers AVID 102, statut confirmé conservé, puis Annuler la restaure exactement dans AVID 103. La release `0.5.0-rc6` reste la base stable ; ce candidat doit passer REVIEW, QA, SECURITY/PERFORMANCE, puis INTEGRATION/E2E avant une nouvelle release.

Décision RELEASE post-RC5 2026-08-25 : **APPROVED — Planify `0.5.0-rc6`**. Le candidat applicatif exact `e39b9b0e2eecf7a0c9abeb0f20ec27650778b09f` et ses rapports passent REVIEW, QA, SECURITY, PERFORMANCE, INTEGRATION et E2E à 0 P0/P1. RC6 livre l'alignement vertical responsive du Planning et la couleur Client persistée/accessible. Preuves terminales : suite complète `345/345`, ciblés QA `74/74`, lint/build/diff-check PASS ; scroll réel synchronisé `0 ↔ 6456`, couleur `#E64A7A` relue après redémarrage et lecteur sans mutation Client. Le rollback vers `v0.5.0-rc5` est applicatif et sans migration destructive. Cette release reste locale : aucune publication ni aucun déploiement distant n'est effectué.

INTEGRATION/E2E post-release RC5 2026-08-25 : **APPROVED — 0 P0 / 0 P1** sur le candidat applicatif exact `e39b9b0e2eecf7a0c9abeb0f20ec27650778b09f`. Un serveur isolé sur le port `8224` et une copie privée de la démonstration ont validé le parcours administrateur : la couleur Netflix France a été changée en `#E64A7A`, rendue comme liseré avec le nom Client dans le libellé accessible, puis retrouvée identique après arrêt/redémarrage et reconnexion. Le scroll vertical réel atteint `scrollTop=6456` dans la grille et la colonne Ressources, revient à `0` sur les deux axes et conserve les fenêtres virtualisées attendues. Le profil lecteur ne voit ni « Nouveau client » ni « Modifier le compte ». Le serveur, l'onglet et le fichier temporaire ont été arrêtés/supprimés. REVIEW, QA, SECURITY et PERFORMANCE sont également APPROVED à 0 P0/P1 ; le candidat peut passer au gate RELEASE local suivant.

Gates techniques post-release RC5 2026-08-24 : **REVIEW, QA, SECURITY et PERFORMANCE APPROVED — 0 P0 / 0 P1** sur le candidat exact `e39b9b0e2eecf7a0c9abeb0f20ec27650778b09f`. Les P1 responsive/OpenAPI et le P2 de validation tronquée sont fermés. Preuves indépendantes : Clients `11/11`, Planning `46/46`, Fondations/OpenAPI `17/17`, ciblés QA `74/74`, suite complète `345/345`, lint/build/diff-check PASS. La couleur invalide avec suffixe renvoie `422` sans mutation ; le `ResizeObserver` est unique, observe grille et shell puis se déconnecte au bind suivant. P2 non bloquants suivis : réponses Client OpenAPI encore génériques et rémanence mémoire locale possible de l'observateur après logout jusqu'au prochain bind, sans DOM visible ni fuite API. Le candidat passe à **INTEGRATION/E2E REQUIS** avant toute prochaine release.

Retour DEV post-release RC5 après gates 2026-08-24 : le candidat `ea7863c` a été rejeté en REVIEW (0 P0/2 P1), tandis que SECURITY et PERFORMANCE étaient approuvés et QA non terminale. Les deux P1 sont corrigés : un `ResizeObserver` unique, déconnecté à chaque nouveau bind, recalcule la hauteur réelle de scrollbar lors des changements responsive ; OpenAPI documente désormais `GET/PATCH /clients/{clientId}` et le schéma versionné `ClientUpdateCommand`, couleur comprise. Le P2 Sécurité est également fermé : la validation ne tronque plus l'entrée à sept caractères avant la regex et refuse explicitement tout suffixe. Tests et gates aval doivent être rejoués sur le nouveau candidat exact avant intégration.

Retour DEV post-release RC5 2026-08-24 : **EN COURS — candidat non publié basé sur `0.5.0-rc5`**. Deux demandes PO sont implémentées sans modifier les données de démonstration : (1) le Planning mesure la hauteur native de sa barre horizontale et l'applique à la colonne Ressources, ce qui supprime l'écart de 14 px et l'accrochage en fin de scroll vertical ; la preuve navigateur sur 75 ressources atteint `scrollTop=6149,5` dans les deux panneaux, puis revient à `0`, avec fenêtres virtuelles cohérentes ; (2) un opérateur peut choisir une couleur hexadécimale sur la fiche Client, validée strictement par le serveur, puis affichée comme liseré sur ses réservations. Le nom du Client est ajouté au libellé accessible et le statut reste textuel, la couleur n'étant jamais le seul signal. L'API/OpenAPI et les tests négatifs rejettent toute couleur non conforme. Preuves DEV fraîches : Clients `11/11`, Planning `46/46`, suite complète `345/345`, syntaxe, lint, build et diff-check PASS. La release `0.5.0-rc5` reste la base stable ; ce nouveau candidat doit repasser REVIEW, QA, SECURITY/PERFORMANCE, INTEGRATION et E2E indépendants avant une prochaine release.

Décision RELEASE RC5 2026-08-24 : **APPROVED — Planify `0.5.0-rc5`**. Tous les gates SPEC, DEV, REVIEW, QA, SECURITY/PERFORMANCE, INTEGRATION et E2E portent sur le candidat applicatif exact `4e094d589ae215f31152110d30f1163929ca1338`, complété uniquement par les rapports et métadonnées de release. La release livre le scroll stable des vues Mois/3 mois, quatre vues Planning sans 6 semaines, le détail Pilotage modal et localisé, le Forecast Direction 30/60/90 et la réconciliation exacte de `occupancyGap`. Preuves terminales fraîches sur les métadonnées finales : suite complète `345/345`, lint/build/syntaxe/diff-check PASS ; smoke isolé Node `v26.6.0` sur le port `8222`, page `200`, connexion administrateur `200`, jeton CSRF présent et 45 permissions chargées. Les mesures applicatives restent Exploitation p95 `20,92 ms` et drill-down p95 `41,21 ms` ; le parcours navigateur Planning/Pilotage/Projet ne présente aucun P0/P1. Le rollback applicatif vers `v0.5.0-rc4` ne requiert aucune migration de données et est documenté dans `README.md`. Cette release est locale : aucune publication ni aucun déploiement distant n’est effectué.

INTEGRATION/E2E RC5 2026-08-24 : **APPROVED — 0 P0 / 0 P1** sur l’applicatif `4e094d589ae215f31152110d30f1163929ca1338`. Serveur 8080 redémarré sur la base de démonstration isolée, connexion et reconnexion réussies, console propre. Mois et 3 mois atteignent gauche/droite avec 92 dates et 3 956 cellules stables ; 6 semaines est absent. Projet → Le Grand Format rend deux cartes 58 px séparées. Direction affiche trois cartes Forecast 30/60/90 sans clés techniques ; Exploitation ouvre un dialogue 48 lignes localisées, un état réel vide sans faux bps, puis restaure le focus. La limitation Échap de l’outil d’automatisation est P2, la fermeture bouton/focus étant prouvée. Ce gate est intégré à la release locale `0.5.0-rc5`.

Gates indépendants RC5 terminaux 2026-08-24 : **REVIEW, QA, SECURITY et PERFORMANCE APPROVED — 0 P0 / 0 P1** sur le candidat applicatif exact `4e094d589ae215f31152110d30f1163929ca1338`. Les deux retours DEV `occupancyGap` sont fermés : périodes sans réalisé exclues symétriquement et moyenne directe des écarts avec un seul arrondi (`[0,-833] → -416 bps`). Preuves : Dashboards `14/14`, Planning `46/46`, ciblés QA `135/135`, suite complète `345/345`, lint/build/diff-check PASS ; Exploitation p95 `20,92 ms`, drill-down `occupancyGap` p95 `41,21 ms`. Le navigateur indépendant n’était pas exposé lors des gates ; INTEGRATION/E2E doit reprendre la recette visuelle réelle avant RELEASE RC5.

Retour DEV RC5 après re-REVIEW 2026-08-24 : le candidat `ace4048` a fermé le sous-ensemble incohérent, mais REVIEW et QA ont identifié un écart résiduel d’un point de base dû à `round(moyenne réelle) - round(moyenne planifiée)`. Le KPI moyenne désormais directement chaque écart `réel - planifié`, puis arrondit une seule fois comme son drill-down. Le négatif pérenne couvre deux périodes réalisées (1 h/1 h puis 3 h/1 h) : lignes `[0,-833]`, moyenne et carte `-416 bps`. Preuves DEV fraîches : Dashboards `14/14`, suite complète `345/345`, lint/build/diff-check PASS. Les rapports sur `ace4048` restent historiques ; re-gates impactés requis sur le nouveau hash.

Retour DEV RC5 après REVIEW 2026-08-24 : le gate indépendant sur `b715f4b` a rejeté le candidat avec `REV-RC5-01` (0 P0/1 P1) : la carte `occupancyGap` soustrayait la moyenne planifiée de toutes les périodes à la moyenne réelle des seules périodes réalisées, alors que son drill-down ne contient que ces dernières. La carte calcule désormais ses deux moyennes sur le même sous-ensemble. Le test négatif mélange deux périodes planifiées (1 h et 8 h) dont une seule réalisée et exige valeur carte = moyenne du détail = `0 bps`. Preuves DEV fraîches : Dashboards `14/14`, suite complète `345/345`, lint/build/diff-check PASS. Toute approbation sur `b715f4b` est invalidée ; re-REVIEW et gates aval impactés requis sur le nouveau hash.

Consolidation RC5 DEV 2026-08-24 : le candidat exact `d96281e0caf86777cdc21eba3ece9ab516420ddf` regroupe les corrections post-RC4 de scroll horizontal, la suppression de la vue 6 semaines, le détail Pilotage réconcilié et le Forecast métier. Le dépôt est propre ; Planning + Dashboards `59/59`, suite complète `344/344`, lint/build/diff-check PASS. Le benchmark Finance représentatif (250 ressources, 10 000 réservations, 2 000 documents/réalisés/coûts) reste sous le seuil lecture de 300 ms : Forecast p95 `49,84 ms`, drill-down facturable p95 `210,66 ms`. Le candidat est prêt pour les gates indépendants REVIEW, QA et SECURITY/PERFORMANCE ; aucun verdict antérieur n’est réutilisé comme approbation de ce hash.

Décision DEV Forecast Pilotage post-RC4 2026-08-24 : **corrigé localement, re-gates indépendants requis avant RC5**. La section technique `forecast` n’imprime plus les clés API `days/through/scheduledMinor/unscheduledMinor/totalMinor`. Le tableau Direction présente trois cartes métier « Horizon 30/60/90 jours », la date de fin française, le total en euros et sa ventilation « Déjà planifié / À planifier ». Le calcul `FINANCE_FORECAST@1`, les scopes, l’API et la persistance restent inchangés. Preuves DEV fraîches : Sprint 8 `13/13`, suite complète `344/344`, lint/build/diff-check PASS ; recette navigateur visuelle à reprendre au gate aval, le navigateur contrôlable n’étant pas exposé à cette passe.

Décision DEV détail Pilotage post-RC4 2026-08-24 : **corrigé localement, re-gates indépendants requis avant RC5**. Le drill-down n’est plus injecté en bas du tableau de bord : il s’ouvre dans un dialogue modal, ferme par bouton, fond ou Échap, restaure le focus et présente des libellés/états/valeurs en français. Les KPI `actualOccupancy` et `occupancyGap` filtrent désormais les périodes sans réalisé ; le total du détail est identique au `sourceCount` de la carte et aucune ligne artificielle `— bps` n’est produite. Recette navigateur : Occupation réelle sans source affiche un état vide dans le dialogue ; Occupation planifiée affiche 48 lignes, pourcentages et « Sous-utilisé », sans identifiant d’unité ou statut anglais ; le bouton Fermer restaure le focus sur la carte. Tests G8 ciblés `17/17`, suite complète `344/344`, lint/build/diff-check PASS.

Décision DEV scroll horizontal post-RC4 2026-08-24 : **corrigé localement, re-gates indépendants requis avant RC5**. Les vues Mois/3 mois utilisaient des largeurs CSS et virtuelles divergentes, et la piste changeait à chaque fenêtre ; les dernières dates devenaient inatteignables puis une accroche subsistait. Le lot centralise les largeurs, fixe l’étendue totale et garde toutes les colonnes montées tout en conservant la virtualisation verticale. La vue Mois couvre trois mois glissants détaillés : précédent, sélectionné centré, suivant ; la vue 3 mois reste compacte. La vue 6 semaines, devenue redondante, est supprimée : quatre commandes restent, Jour/Semaine/Mois/3 mois. Recette navigateur : quatre boutons présents et 6 semaines absent ; Mois `01/07→30/09`, position centrée, gauche/droite accessibles ; 12 déplacements successifs en `337 ms`, DOM constant à 92 dates/4 508 cellules et aucune erreur console. Preuves DEV : Planning `46/46`, complet `343/343`, lint/build/diff-check PASS. Aucun contrat API, donnée, permission ni persistance modifié.

Décision RELEASE vue Projet post-RC3 2026-08-24 : **APPROVED — Planify `0.5.0-rc4`** sur le candidat applicatif exact `2fd37e212d19ecc507cfe12f077474f716ec0edd`. Le parcours `Projet → Ouvrir le planning` compacte les cartes empilées à `58 px`, agrandit uniformément les lignes jusqu’à trois cartes (`92/132/194 px`), puis conserve les cartes supplémentaires dans une cellule à défilement local. Les piles heure/demi-journée sont confinées, les réservations hors 09:00–18:00 restent exclues et une réservation isolée conserve sa largeur de durée. Le DOM est borné à 50 cartes par cellule avec compteur accessible ; l’index `ressource × créneau` remplace les rescans par case. La hauteur publiée est relue par le scroll, les axes et la virtualisation restent synchronisés. Recette navigateur Le Grand Format : deux cartes de 58 px dans une ligne de 132 px, sans couverture. REVIEW, QA, SECURITY et PERFORMANCE approuvent sans P0/P1 ; ciblés jusqu’à 151/151, suite 341/341, lint/build/diff-check PASS. Benchmarks : 10k distribué p95 `40,63 ms`, long 92 jours `1 064,73 ms`, sous 2 s pré-DOM. Aucun contrat API ni donnée n’est modifié.

Décision RELEASE Planning post-RC2 2026-08-24 : **APPROVED — Planify `0.5.0-rc3`**. Le défaut de scroll vertical est fermé : les réservations temporisées (`z-index:4`) et focalisées (`9`) passent sous les dates sticky (`10`), tandis que la colonne Ressources (`11`) et son angle (`12`) restent au premier plan sur l’axe horizontal. La recette navigateur reproduit le cas exact à `scrollTop=70`, confirme les dates lisibles et une console sans erreur. REVIEW, QA, SECURITY et PERFORMANCE approuvent le candidat `56b9f45…` sans P0/P1 ; ciblés 60/60 et QA 150/150, suite 340/340, lint/build/diff-check PASS. Aucune géométrie, virtualisation, API, donnée ou permission n’est modifiée.

Décision RELEASE visuelle G8 2026-08-24 : **APPROVED — Planify `0.5.0-rc2`**. Le candidat applicatif `34a9d7883dcf22cad517bf45393848eaa60d48d8` ferme le défaut de tokens et le contraste focus révélé par la recette PO de RC1. Les six onglets Pilotage sont lisibles (`4,86:1`), le focus clavier atteint au minimum `4,16:1`, Réalisations et Finance récupèrent leurs contours, Planning reste inchangé et la console navigateur est propre. REVIEW, QA, SECURITY et PERFORMANCE sont à 0 P0/P1 ; Foundations 17/17, ciblés QA 107/107, suite 340/340, lint/build/diff-check PASS. `package.json`, `CHANGELOG.md`, `README.md` et l’addendum E2E sont alignés ; aucune publication distante n’est effectuée.

Décision recette visuelle PO post-release 2026-08-24 : la recette réelle de `0.5.0-rc1` a identifié un défaut de contraste transversal : les modules Pilotage, Réalisations et Finance utilisaient cinq variables CSS sémantiques non définies, notamment l’onglet Pilotage actif rendu blanc sur fond transparent clair. Les alias canoniques `primary/surface/surface-soft/text/border` sont désormais reliés au design system existant et protégés par un test de fondation. La release locale `0.5.0-rc1` reste la baseline précédente ; ce correctif visuel revient en DEV et doit être contrôlé dans le navigateur puis repasser les gates impactés avant une candidate `0.5.0-rc2`.

Décision retour DEV focus RC2 2026-08-24 : la re-REVIEW du correctif de tokens a mesuré le contour clavier Pilotage à `1,62:1`, sous la cible non textuelle `3:1`. Le mélange transparent est remplacé par le violet primaire opaque, mesuré à `4,86:1` sur blanc ; l’offset et l’épaisseur restent inchangés. Une assertion protège le sélecteur final. Re-REVIEW, re-QA et revalidation d’impact SECURITY/PERFORMANCE sont requises avant RC2.

Décision RELEASE G8 2026-08-24 : la candidate locale **`0.5.0-rc1`** consolide les dashboards, exports, API Analytics/BI et le durcissement final des surfaces authentifiées sur le candidat applicatif `68489b1`. `package.json`, `CHANGELOG.md`, `README.md` et la procédure de rollback Sprint 8 sans migration sont alignés. Les preuves terminales sont REVIEW/QA/SECURITY/PERFORMANCE/INTEGRATION/E2E à 0 P0/P1, suite complète 339/339, lint/build/diff-check PASS, six dashboards et profil lecteur contrôlés dans le navigateur, logout/401/reconnexion fail-closed et console propre. La publication ou le déploiement distant ne font pas partie de cette release locale.

Décision terminale G8 2026-08-24 : **APPROVED — 0 P0 / 0 P1** sur le candidat applicatif exact `68489b1fc0575706ecbf13c191ab033dc1981d63`. REVIEW, QA, SECURITY et PERFORMANCE approuvent le même contenu ; Foundations 16/16, ciblés QA 106/106, suite complète 339/339, lint/build/diff-check PASS. Le parcours Integration/E2E confirme les six dashboards, filtres URL, drill-down réconcilié, export Excel, profil lecteur sans coûts/marges et console propre. Le cycle startup/login/logout/401/reconnexion est fail-closed sur les 24 routes composées : shell et trois overlays privés sont masqués et `inert`, le DOM métier est purgé et le focus revient au login avant reconstruction authentifiée. G8 franchit SPEC, DEV, REVIEW, QA, SECURITY/PERFORMANCE, INTEGRATION et E2E ; prochaine étape : gate RELEASE local, version et notes de livraison.

Décision SPEC Sprint 8 2026-08-24 : le Sprint 8 « Dashboards, exports & sécurité finale » est lancé depuis les douze stories exactes `US-094` à `US-099`, `US-101` à `US-104`, `US-107` et `US-109` (86 points). La spécification `docs/specifications/sprint-8-dashboards-reporting.md` fixe six dashboards adaptés aux rôles, des exports Planning/KPI Excel et PDF, une API Analytics/BI locale, la confidentialité complète des coûts/marges et la preuve finale des overrides motivés. Les KPI partagent définition, `asOf`, filtres, fraîcheur et drill-down ; les scopes sont appliqués avant agrégation. La valeur « facturé » reste explicitement indisponible tant qu’aucune source comptable n’existe, sans faux zéro. Ordre DEV : S8-A dashboards, S8-B exports, S8-C BI, S8-D sécurité finale. G8 reste **BLOQUÉ** jusqu’aux gates indépendants et au parcours E2E réconciliant écran, export et API.

Décision DEV Sprint 8 — S8-A 2026-08-24 : les six tableaux de bord Direction, Finance, Planning, Commercial, Exploitation et Chef de projet sont implémentés comme read-models serveur versionnés. Les permissions et scopes Société/Site/Projet/Client/Devis/Ressource sont appliqués avant les agrégats ; les coûts et marges ne sont jamais envoyés sans `finance.read`. Chaque KPI fournit définition, statut, valeur/unité, devise pour les montants, nombre de sources et drill-down. « Facturé » et « Encaissé » restent explicitement indisponibles. La page `#pilotage` adapte les onglets aux droits et expose les états vides/indisponibles sans faux zéro. Preuves DEV fraîches : S8-A 6/6, API 42/42, régressions Finance G7 27/27, suite complète 318/318, lint/build/diff-check PASS ; smoke navigateur administrateur sur Direction et Finance, 9 cartes Finance, états indisponibles explicites, aucune erreur console. S8-A est prêt à figer ; S8-B exports démarre ensuite. G8 reste **BLOQUÉ** jusqu’aux quatre incréments et gates indépendants.

Décision DEV Sprint 8 — S8-B 2026-08-24 : les exports Planning Excel, Planning PDF et KPI Excel sont implémentés localement, sans dépendance ni réseau. Les exports reprennent la période et les filtres Site/Projet visibles, appliquent permissions et scopes avant toute ligne, échouent fermés sur un Projet masqué et bornent la période à 366 jours et le classeur à 10 000 lignes. Le XLSX neutralise les préfixes de formule, fige l’en-tête et fournit un filtre ; le PDF paysage répète titre, période, colonnes et pagination. Le classeur KPI appelle exactement le read-model du dashboard et conserve valeur brute, unité, devise, exposant, définition, statut, sources et drill-down. Preuves DEV fraîches : S8-A+B 10/10, API + Sprint 8 52/52, suite complète 322/322, lint/build/diff-check PASS ; XLSX relu avec `openpyxl`, PDF relu avec `pypdf` et rendu visuel Poppler sur deux pages ; benchmark génération 10 000 lignes `164,82 ms`. Les trois boutons sont visibles en navigateur sans erreur console. S8-B est prêt à figer ; S8-C API BI est la prochaine étape. G8 reste **BLOQUÉ** jusqu’à S8-C/S8-D et aux gates indépendants.

Décision DEV Sprint 8 — S8-C 2026-08-24 : l’API Analytics/BI locale expose un catalogue fermé de dix datasets et une lecture `/api/v1/analytics/datasets/:dataset` en JSON paginé ou CSV UTF-8. Les champs et versions de définition sont stables ; `pageSize` est borné à 500 et une requête dépassant 10 000 lignes exige une partition explicite. Les permissions sont évaluées par dataset, puis les scopes Société/Site/Projet/Client/Devis/Réservation/Ressource sont appliqués avant les read-models canoniques. Les réalisés retirent les coûts sans `finance.read`, le catalogue d’un Planificateur exclut les datasets Finance, les dates futures et formats inconnus échouent avec des erreurs stables, et le CSV neutralise les préfixes de formule. OpenAPI documente les paramètres, les dix valeurs, JSON/CSV et les enveloppes. Preuves DEV fraîches : S8-C 6/6, suite complète 328/328, lint/build/diff-check PASS ; contrôle des 157 références OpenAPI et paramètres de chemin PASS. S8-C est terminé ; S8-D sécurité finale est la prochaine étape. G8 reste **BLOQUÉ** jusqu’à S8-D, gates indépendants et réconciliation E2E.

Décision DEV Sprint 8 — S8-D 2026-08-24 : la dérogation de conflit est centralisée sur tous les parcours Réservation (création, duplication, cellule, batch, restauration, déplacement, redimensionnement et mise à jour). En présence d’un conflit, `planning.override_conflict` est obligatoire, le motif dédié contient 3 à 500 caractères, la dérogation et son motif sont persistés, et l’audit canonique contient `before/after`, versions, conflits, `operationId` et origine. Les replays réévaluent le droit courant ; un replay exact ne réémet aucun audit/événement et une divergence répond `409`. La validation UI est alignée. La matrice des sept rôles standards, six dashboards et trois exports confirme que seuls ADMIN/FINANCE portent `finance.read`, que le Planificateur n’accède ni aux dashboards/datasets Finance ni à l’audit, et que ses exports Planning restent sans coûts/marges. Preuves DEV fraîches : S8-D 3/3, Sprint 8 + API 61/61, suite complète 331/331, lint/build/diff-check PASS. S8-A/B/C/D sont terminés ; G8 reste **BLOQUÉ** jusqu’aux gates REVIEW, QA, SECURITY/PERFORMANCE, puis INTEGRATION/E2E sur le même candidat.

Décision gates G8 2026-08-24 : le candidat exact `0732150a9816cb3139282fabbd9bd6e3c3fe2a0a` est **BLOQUÉ — retour DEV requis**. REVIEW et QA concluent `CHANGES REQUIRED` avec 0 P0 et 4 P1 : compléter et réconcilier les six dashboards avec filtres et drill-down exploitables ; produire les exports XLSX attendus avec synthèse, détail, filtres et définitions, ainsi qu’un Planning ventilé allocation/jour ; borner le PDF Planning à 62 jours avec orientation adaptée, fuseau et légende ; protéger les données Maintenance par `maintenance.read` et remplacer la matrice déclarative par une preuve réelle 7 rôles × 6 dashboards × 3 exports ; enfin, empêcher le replay exact de `duplicateReservationCell` de réémettre `quote.planningProgress`. SECURITY et PERFORMANCE sont `APPROVED`, 0 P0/P1 ; pire dashboard p95 `123,67 ms`, XLSX 10 000 lignes `153,40 ms`, PDF 10 000 lignes/62 jours `370,38 ms`. Les preuves de non-régression restent vertes : Sprint 8 + API 61/61, suite complète 331/331, lint/build/OpenAPI/diff-check PASS. Après correction, re-REVIEW, re-QA et gates aval impactés sont obligatoires sur un nouveau candidat figé.

Décision retour DEV G8 2026-08-24 : les quatre P1 REVIEW/QA sont corrigés sur un nouveau candidat à figer. Les six dashboards exposent les KPI attendus, conservent les filtres Site/Projet/Client/Commercial/Ressource/Catégorie et fournissent un drill-down paginé réévalué avec les permissions et scopes courants ; l’interface Pilotage ouvre ce détail sans perdre le contexte. Les exports KPI comportent les feuilles `Synthèse`, `Détail` et `Définitions`; l’export Planning comporte `Planning`, `Filtres` et `Définitions`, avec une ligne par allocation et jour et de vraies dates Excel. Le PDF est limité à 62 jours, utilise A4 paysage jusqu’à 14 jours puis A3, et indique fuseau, filtres et légende. Exploitation exige désormais `maintenance.read`; une matrice HTTP effective couvre sept rôles × six dashboards × trois exports. Enfin, le rejeu exact d’une duplication de cellule liée à un Devis ne réémet plus `quote.planningProgress`. Preuves DEV fraîches : ciblés G8 + API 64/64, suite complète 334/334, lint/build/diff-check PASS. Empreintes : serveur `015388c5…`, frontend `c40d6bb1…`, OpenAPI `c4adb3ef…`, tests Dashboards `2fe0fa87…`, Exports `45b0eb8e…`, Sécurité `4258f8e2…`. G8 reste **BLOQUÉ — RE-GATES REQUIS** : REVIEW, QA puis SECURITY/PERFORMANCE impactés doivent statuer indépendamment sur ce candidat avant INTEGRATION/E2E.

Décision re-gates G8 2026-08-24 : le candidat exact `1d4d97b3c43b6d91756b5c74207371dd879c760a` reste **BLOQUÉ — retour DEV requis**. REVIEW conclut `CHANGES REQUIRED` avec 3 P1 : dashboards encore partiellement non réconciliables et sections non rendues/exportées ; détail KPI silencieusement borné à 10 000 lignes et période Planning acceptant 367 jours inclusifs ; matrice 7×6×3 insuffisamment exercée via les endpoints réels. QA conclut `REJECTED` avec 1 P1 reproduit : le filtre Projet n’est pas appliqué au calcul d’occupation (`3 750 bps` observés contre `417 bps`) et le KPI hebdomadaire Direction ne se réconcilie pas avec son détail journalier (`3 333 bps` contre `2 000 bps`). SECURITY est `APPROVED`, 0 P0/P1. PERFORMANCE est `REJECTED` avec 1 P1 : le drill-down public sans `kpiId` calcule tous les indicateurs et atteint environ `560 ms` p95, au-dessus du seuil `<300 ms`; un KPI explicite reste conforme à `264,50 ms` au pire. Les fermetures Maintenance, structure principale XLSX/PDF et replay SSE sont confirmées. Preuves fraîches : ciblés 64/64, suite complète 334/334, lint/build/OpenAPI/diff-check PASS. Condition de déblocage : correction DEV de ces P1, nouveaux négatifs de réconciliation/borne/matrice/performance, puis re-REVIEW, re-QA et re-PERFORMANCE sur un candidat figé.

Décision second retour DEV G8 2026-08-24 : les P1 du candidat `1d4d97b3…` sont corrigés et un nouveau candidat doit être figé puis soumis aux re-gates. L’occupation applique désormais les filtres Projet, Client et Commercial avant agrégation et les cartes utilisent les mêmes lignes journalières que leur drill-down ; la sonde Projet obtient `417 bps` sur la carte comme au détail. Les sections Forecast/Pipeline et les écarts Projet sont rendus et exportés dans une feuille `Sections`; le détail est paginé, ses filtres sont partageables par URL, les remises exposent leur poids et les volumes supérieurs à 10 000 sources échouent explicitement au lieu d’être tronqués. Le Planning XLSX refuse 367 jours inclusifs. Le drill-down public exige un `kpiId` et répond immédiatement `422 DASHBOARD_KPI_REQUIRED` lorsqu’il manque, fermant le chemin p95 à environ `560 ms`. La matrice de sécurité appelle réellement les trois endpoints pour sept rôles et six dashboards (`126` contrôles HTTP), plus les exports Planning. Preuves DEV fraîches : ciblés G8 + API `67/67`, dashboard `11/11`, suite complète `337/337`, lint/build/diff-check PASS ; benchmark Finance représentatif 250 ressources/10 000 réservations/2 000 Devis/2 000 réalisés/2 000 coûts, pire p95 `76,60 ms` et occupation annuelle `30,68 ms`. Empreintes : serveur `9c76d64f…`, frontend `88970864…`, OpenAPI `7395603e…`, tests Dashboard `fdcd75f7…`, Exports `7570ca69…`, Sécurité `9c08bff3…`. G8 reste **BLOQUÉ — RE-REVIEW, RE-QA ET RE-PERFORMANCE REQUIS** sur le même commit avant INTEGRATION/E2E.

Décision re-gates G8 sur `33ec24b` 2026-08-24 : G8 reste **BLOQUÉ — retour DEV ciblé**. REVIEW et QA ferment tous les P1 historiques mais identifient un P1 commun : le dashboard Projet compte des réalisés hors période, ce qui peut afficher `100 %` et aucun écart alors qu’une Réservation visible reste sans réalisé ; carte et détail divergent. SECURITY est `APPROVED`, 0 P0/P1. PERFORMANCE ferme le chemin sans `kpiId` (`422`, p95 `193,33 ms`) et confirme les six dashboards sous `300 ms`, mais reste `REJECTED` avec `PERF-G8-03` : le drill-down Finance explicite `billableRevenue` atteint `331,53 ms` p95. Preuves communes : ciblés `67/67`, suite complète `337/337`, lint/build/OpenAPI/diff-check PASS ; matrice HTTP réelle `126`. Conditions de déblocage : dériver les réalisés Projet uniquement des Réservations visibles dans la fenêtre et optimiser le drill-down `billableRevenue` sous `300 ms`, ajouter les négatifs/mesures correspondants, puis rejouer REVIEW, QA et PERFORMANCE sur un nouveau commit exact.

Décision troisième retour DEV G8 2026-08-24 : les deux derniers P1 sont corrigés sur un nouveau candidat à figer. Les KPI Projet dérivent désormais leurs réalisés des seules Réservations visibles, de leur révision courante intersectant la période et confirmée au plus tard à `asOf`; l’avancement utilise le nombre distinct de Réservations réalisées. Le négatif combine un réalisé ancien hors fenêtre et un réalisé futur : pour la Réservation visible, `planning=1`, `actuals=0`, `actualCompletion=0 bps`, `actualGap=1`, identiques au drill-down. Le drill-down Finance réutilise en mémoire le résultat autorisé et non énumérable déjà calculé par le dashboard au lieu de recalculer `financeUnbilledOverages`; aucune donnée interne supplémentaire n’est sérialisée. Le benchmark contractuel Finance mesure maintenant explicitement `billableDrilldown` sur 250 ressources/10 000 réservations/2 000 Devis/2 000 réalisés/2 000 coûts : p95 `201,52 ms`, contre `331,53 ms` au gate précédent et sous le seuil `<300 ms`. Preuves DEV fraîches : ciblés G8 + API `68/68`, dashboard `12/12`, suite complète `338/338`, lint/build/diff-check PASS. Empreintes : serveur `8bf91bc8…`, tests Dashboard `aa416fc5…`, benchmark Finance `087702c7…`. G8 reste **BLOQUÉ — RE-REVIEW, RE-QA ET RE-PERFORMANCE REQUIS** sur le prochain commit exact.

Décision re-gates G8 sur `b56d13f` 2026-08-24 : SECURITY et PERFORMANCE sont **APPROVED**, 0 P0/P1 ; `billableRevenue` passe à `206,60 ms` p95 sur le benchmark permanent et `268,50 ms` sur 30 mesures indépendantes, tandis que l’absence de KPI échoue en `422` à `183,45 ms` p95. REVIEW et QA restent **BLOQUÉS** par un unique P1 : `dashboardActualRows` n’exige pas encore que `actualRecord.sourceReservationVersion` égale la version courante de la Réservation visible. Une Réservation `version=2` avec uniquement un réalisé source `version=1` affiche encore `actuals=1`, `actualCompletion=10000`, `actualGap=0`; attendu `0`, `0`, `1`. Tous les autres contrôles passent : ciblés `68/68`, suite `338/338`, lint/build/OpenAPI/diff-check, matrice `126`, pagination/cache Finance et exports. Correction DEV minimale puis re-REVIEW/re-QA requises ; les gates Sécurité/Performance restent valides si le diff est strictement limité à cette comparaison et son test.

Décision quatrième retour DEV G8 2026-08-24 : le dernier P1 REVIEW/QA est corrigé par une comparaison stricte `actualRecord.sourceReservationVersion === reservation.version` dans l’ensemble canonique des réalisés Dashboard. Le négatif couvre successivement une Réservation visible v1 avec réalisé futur, son passage en v2 avec uniquement le réalisé v1 obsolète (`actuals=0`, avancement `0`, écart `1`), puis l’ajout du réalisé v2 courant (`actuals=1`, avancement `10000`, écart `0`), carte et drill-down identiques. Le diff produit est limité au serveur et au test Dashboard ; les gates SECURITY/PERFORMANCE approuvés sur `b56d13f` ne sont pas impactés. Preuves DEV fraîches : Dashboard `12/12`, suite complète `338/338`, lint/build/diff-check PASS. Empreintes : serveur `b287ee5a…`, tests Dashboard `098b4f46…`. G8 reste **BLOQUÉ — RE-REVIEW ET RE-QA REQUIS** sur le commit exact à figer.

Décision gates techniques G8 finaux 2026-08-24 : le candidat exact `5f2b7d13dc034735f26c9c54dcead2a51fc20d6f` est **APPROVED pour REVIEW, QA, SECURITY et PERFORMANCE**, chacun à 0 P0/P1. Les scénarios version obsolète/courante sont identiques entre cartes et drill-down ; le cache Finance reste borné à la requête, non énumérable et non sérialisé. Preuves terminales : ciblés `68/68`, suite complète `338/338`, lint/build/OpenAPI/diff-check PASS ; benchmark représentatif `billableDrilldown` p95 `214,76 ms` en REVIEW et `206,60 ms` au gate Performance, confirmation indépendante p95 `268,50 ms`. SECURITY conserve un P2 non bloquant sur la matérialisation avant plafond d’export ; REVIEW conserve un P2 accessibilité sur le pattern d’onglets/focus. G8 passe à **GATES TECHNIQUES APPROUVÉS — INTEGRATION/E2E REQUIS** sur le même candidat avant RELEASE.

Décision retour DEV issue de l’E2E G8 2026-08-24 : le parcours navigateur des six dashboards, filtres URL, drill-down, export et profil lecteur est fonctionnel, mais la déconnexion/reconnexion révélait un chevauchement visuel : l’écran de connexion s’affichait au-dessus de l’ancien shell administrateur. Le correctif initialise désormais `#appShell` masqué et le rend `hidden`, `aria-hidden` et `inert` tant qu’aucune session n’est authentifiée. La preuve navigateur isolée confirme : démarrage hors session avec écran de connexion seul, shell visible après login, disparition immédiate du shell au logout, puis écran de connexion seul après rechargement ; console sans erreur. Une non-régression statique a été ajoutée. Preuves DEV fraîches : Fondations `16/16`, suite complète `339/339`, lint/build/diff-check PASS. Comme ce diff touche `app.js`, `index.html` et `styles.css`, G8 revient à **RE-GATES REVIEW, QA ET SECURITY/PERFORMANCE REQUIS** avant de reprendre INTEGRATION/E2E et RELEASE.

Décision second retour DEV UI G8 2026-08-24 : REVIEW et SECURITY ont confirmé que trois overlays placés hors de `#appShell` pouvaient survivre visuellement et conserver le focus après logout ou expiration `401`. La synchronisation d’authentification couvre maintenant le shell, la modale Réservation, la palette de recherche et le tiroir Stock : tout overlay est fermé puis rendu `inert` hors session, le contenu principal résiduel est supprimé du DOM et le focus revient au champ e-mail. À la reconnexion, les surfaces redeviennent actives sans être ouvertes automatiquement. Le test de fondation couvre ces invariants ; re-REVIEW, re-QA et re-SECURITY sont obligatoires sur le nouveau commit avant E2E final.

Décision troisième retour DEV UI G8 2026-08-24 : l’E2E sur le candidat approuvé `08595fc…` a révélé que la composition historique des wrappers `render()` permettait aux routes spécialisées, notamment Pilotage, de produire leur contenu sans repasser par le rendu de base qui réaffiche le shell. Le point d’entrée `render()` final synchronise désormais systématiquement toutes les surfaces d’authentification avant de déléguer aux modules spécialisés. L’invariant startup/login/logout/expiration s’applique ainsi à toutes les routes, pas seulement Planning/Paramètres. Une assertion de composition protège cet appel terminal. Le candidat revient en DEV et doit repasser les gates impactés avant E2E final.

Décision RELEASE G7 2026-08-24 : la candidate locale **`0.4.0-rc1`** consolide Réalisations et Finance sur le candidat applicatif `7051fe4`, les gates indépendants G7-D et le parcours Integration/E2E. `package.json`, `CHANGELOG.md`, `README.md` et les procédures de rollback Sprint 7 sont alignés. Les preuves terminales sont : REVIEW/QA/SECURITY/PERFORMANCE à 0 P0/P1, suite complète 312/312, lint/build/OpenAPI/diff-check PASS, benchmark représentatif sous les seuils, création et relecture Finance après redémarrage, refus lecteur et console navigateur propre. La publication ou le déploiement distant ne font pas partie de cette release locale.

Décision retour DEV G7-D 2026-08-24 : les P1 du premier passage sont corrigés sur un nouveau candidat à figer. Le moteur déduplique les options par groupe/priorité, conserve le signal planifié en l’absence de réalisé et parcourt seulement les jours intersectés ; la fenêtre annuelle 250 ressources/10 000 réservations passe de `3 235,26 ms` à `36,70 ms` p95. `asOf` borne maintenant les coûts réels et les dépenses Projet sont ventilées dans les axes. Le seuil global exige `organizationScope` avant idempotence/mutation ; le négatif HTTP vérifie l’absence de seuil, marqueur et audit. Le non-facturé expose Réservations et action suggérée ; la comparaison tarifaire distingue le catalogue indisponible, fournit remise et marge pondérées ; les quatre listes sont paginées. Preuves DEV fraîches : S7-D 7/7, API 42/42, suite complète 311/311, lint/build/diff-check PASS. Re-REVIEW, re-QA et re-SECURITY/PERFORMANCE restent obligatoires ; G7 demeure **BLOQUÉ** jusque-là.

Décision gates G7-D 2026-08-23 : **BLOQUÉ — retour DEV requis** sur le candidat applicatif `5f61fd4`. REVIEW : 0 P0/7 P1 ; QA : 0 P0/2 P1 ; SECURITY : 0 P0/1 P1 ; PERFORMANCE : 0 P0/1 P1. Correctifs obligatoires : dédupliquer les options d’un même groupe et conserver le signal planifié lorsqu’aucun réalisé n’existe ; appliquer `asOf` aux coûts réels et intégrer les dépenses Projet aux axes ; interdire le seuil Société aux acteurs limités à un Site ; rendre le non-facturé actionnable et traçable ; calculer remise et marge depuis un catalogue réellement applicable ; remplacer les troncatures silencieuses par une pagination ; ramener l’occupation annuelle sous `300 ms` (mesure actuelle `3 235,26 ms`). Les gates doivent être rejoués sur le prochain candidat.

Décision DEV Sprint 7 — S7-D 2026-08-23 : les stories `US-089` à `US-093` sont implémentées sur le candidat applicatif `5f61fd4` à revalider. Une migration additive `sprint-7-occupancy-profitability-v1` crée les seuils d’occupation société/site versionnés, leurs révisions append-only et l’idempotence, avec sauvegarde privée, contrôle d’intégrité et rollback précédé d’un export. Le moteur réconcilie capacité brute, maintenance/indisponibilité, capacité nette, occupation planifiée et réelle sur des intervalles semi-ouverts ; il classe sous-utilisation/saturation selon le seuil applicable. Les maintenances superposées sont fusionnées par ressource, les périodes sont bornées à 366 jours et la rentabilité agrège toutes les lignes avant pagination. Les read-models exposent aussi rentabilité multidimensionnelle, réalisé non couvert sans l’ajouter au CA signé/facturé, et comparaison pondérée prix figé/catalogue applicable à la date du Devis. Les scopes sont appliqués avant agrégation et la page Finance rend les résultats dans des régions tabulaires accessibles. Preuves DEV fraîches Node v26.6.0 : S7-D 4/4, suite complète 307/307, lint/build/diff-check PASS. Benchmark représentatif 250 ressources/10 000 réservations/2 000 Devis/2 000 réalisés/2 000 coûts : p95 Occupation `29,05 ms`, Rentabilité `25,65 ms`, Non-facturé `45,67 ms`, Remises `7,22 ms` (seuil `<300 ms`). REVIEW, QA, SECURITY/PERFORMANCE indépendants sont requis avant INTEGRATION/E2E et G7 reste **BLOQUÉ**.

Décision SPEC Sprint 7 — S7-D 2026-08-23 : le dernier incrément « Occupation & rentabilité » est lancé sur les stories exactes `US-089` à `US-093`. Le moteur serveur restera l’unique autorité et livrera quatre read-models bornés : capacité/occupation planifiée et réelle avec alertes de sous-utilisation/saturation ; rentabilité par salle, prestation, Client, Projet et Site ; dépassements réalisés non couverts commercialement ; analyse pondérée prix figé/catalogue/remise/marge. Les seuils d’occupation seront versionnés par société/site et leurs mutations resteront séparées des lectures analytiques, avec permission, idempotence, audit, SSE après commit et rollback exportable. Aucun surplus ne sera compté en CA signé, facturé ou encaissé. Les scopes Société/Site/Projet/Client/Devis/Ressource/Réalisé seront appliqués avant chaque total et drill-down. Ordre DEV : (1) seuils + occupation nette, (2) rentabilité, (3) non-facturé/remises, (4) API/UI/tests/benchmark consolidés. G7 reste **BLOQUÉ** jusqu’aux gates indépendants et au parcours E2E global S7-A→D.

Décision INTEGRATION/E2E Sprint 7 — S7-C 2026-08-23 : **APPROVED — 0 P0/0 P1** sur le candidat applicatif `05f65c54851701e2ada724d22fed7987edfeef08` et la consolidation de gates `492e71e`. Un serveur isolé sur `localhost:8215` a validé dans le navigateur la page Finance, les définitions Backlog/Forecast, les fenêtres 30/60/90, l’état vide explicite, le rechargement avec reconnexion et le refus complet du rôle lecteur sans `finance.read`, sans erreur console ni donnée utilisateur. Les cas chiffrés non nuls sont couverts sur le même candidat par les tests indépendants (`asOf`, centimes, Devis/version/ligne, complément accepté, périodes revenue-chain) et la suite 303/303. Limite P2 : le seed navigateur ne contenait aucun Devis accepté ; le drill-down non nul sera repris dans la recette globale G7. **S7-C est intégré et validé E2E** ; l’étape suivante est S7-D « occupation & rentabilité », dernier incrément avant G7.

Décision gates Sprint 7 — S7-C 2026-08-23 : **REVIEW, QA, SECURITY et PERFORMANCE APPROVED** sur le commit applicatif exact `05f65c54851701e2ada724d22fed7987edfeef08`, avec 0 P0/0 P1. Les quatre P1 initiaux sont fermés : `asOf` borne les réalisés et la chaîne CA ventile les sources à leurs dates ; les arrondis conservent exactement le montant restant ; le drill-down relie Devis, version et ligne dans un OpenAPI structuré ; les compléments acceptés couvrent le dépassement avant tout `billable`, sans double backlog. Preuves indépendantes : ciblés jusqu'à 83/83, suite complète 303/303, lint/build/diff-check et OpenAPI sémantique PASS. Benchmark représentatif 250 ressources/10 000 réservations/2 000 Devis/2 000 réalisés/2 000 coûts : p95 Marges `27,47 ms`, Backlog `76,13 ms`, Forecast `71,48 ms`, seuil `<300 ms`. Les P2 documentés restent non bloquants. S7-C passe en INTEGRATION/E2E ; G7 reste bloqué jusqu'à S7-D.

Décision INTEGRATION/E2E Sprint 7 — S7-B 2026-08-23 : le candidat applicatif `37a133762bc7626cc9b51bc9577a52a44c3820ec`, consolidé par `3a0367147b258c643c211f7de8b62107c2467e8b`, a été rejoué avec une persistance isolée sur `localhost:8214`. Dans l'interface Client, l'administrateur voit « Importer Excel » et ouvre le dialogue accessible d'analyse ; le planificateur, qui conserve `client.manage` sans `finance.cost.manage`, ne voit aucun bouton d'import et reçoit le message explicite de restriction. La route Finance affiche « Accès refusé » sans coût ni marge pour ce rôle. Après rechargement et reconnexion, ces restrictions restent identiques ; aucune erreur console n'a été observée et aucune donnée de travail n'a été modifiée. Le serveur a été arrêté et tous les artefacts temporaires supprimés. **S7-B est intégré et validé E2E** ; S7-C peut démarrer, tandis que G7 reste en attente de S7-C/S7-D.

Décision gates finaux Sprint 7 — S7-B 2026-08-23 : **REVIEW, QA, SECURITY et PERFORMANCE APPROVED** sur le commit applicatif exact `37a133762bc7626cc9b51bc9577a52a44c3820ec`, avec 0 P0/0 P1. Le parcours d'import de grille client est cohérent avec l'autorité serveur : le bouton « Importer Excel » n'est visible que si l'acteur cumule `client.manage` et `finance.cost.manage`; ouverture, prévisualisation et confirmation répètent la garde côté navigateur. Preuves fraîches : Clients+Finance 24/24, suite complète 297/297, lint/build/diff-check PASS. Les contrôles serveur d'écriture des coûts, les projections financières et les benchmarks restent approuvés. **S7-B passe en INTEGRATION/E2E** ; les P2 documentés restent non bloquants.

Décision correctif sécurité DEV Sprint 7 — S7-B 2026-08-23 : le dernier canal d'écriture SEC-S7B-11 est fermé. Une ligne de devis contenant `costUnitMinor`, la création d'un tarif `/rates` et l'activation d'une grille client importée exigent désormais explicitement `finance.cost.manage` côté serveur, en plus des permissions commerciales ou Client du parcours. Un rôle `quote.manage`/`client.manage` sans Finance reçoit `403 FINANCE_COST_MANAGE_REQUIRED` avant toute mutation, version, audit ou fichier persistant. L'interface omettait déjà le champ et le payload sans cette permission ; OpenAPI et la spécification S7 documentent maintenant l'autorité conjointe. Preuves ciblées fraîches : Finance 13/13, Clients 11/11 ; suite complète et gates aval à rejouer sur le nouveau candidat.

Décision correctif DEV ultime Sprint 7 — S7-B 2026-08-23 : les canaux commerciaux résiduels signalés par SECURITY puis REVIEW sont fermés. Toutes les réponses des routes Devis, versions, catalogue et grilles tarifaires sont projetées selon `finance.read` : les rôles commerciaux conservent les prix de vente nécessaires au devis, sans coûts, snapshots ni marges internes ; les rôles Finance conservent la réponse complète. L'interface masque désormais les outils, indicateurs et onglets de rentabilité sans `finance.read`, et le champ de coût n'est proposé/transmis qu'avec `finance.cost.manage`, sans fabriquer de zéro. OpenAPI décrit la réponse tarifaire conditionnelle via `RateResponse`. Preuves DEV fraîches Node v26.6.0 : Finance 13/13, suite complète 297/297, lint/build/diff-check PASS. Ce nouveau candidat doit repasser REVIEW, QA et SECURITY/PERFORMANCE indépendants avant INTEGRATION/E2E S7-B.

Décision correctif DEV complémentaire Sprint 7 — S7-B 2026-08-23 : les deux canaux adjacents relevés par SECURITY sur `cf89c30…` sont fermés. Les événements historiques de type `rate` suivent désormais la même projection restreinte que `costRate`, `projectCost` et `actualRecord` pour tout auditeur dépourvu de `finance.read`. Le dashboard Projet reste consultable avec `quote.read`, mais n’inclut `estimatedCost`, `estimatedMargin`, `actualCost` et `actualMargin` qu’avec `finance.read`. Les tests négatifs vérifient explicitement le masquage Audit et Dashboard, ainsi que la conservation des champs pour l’administrateur Finance. Preuves DEV fraîches : Finance 11/11, suite complète 295/295, lint/build/diff-check PASS. La PERFORMANCE est déjà approuvée sur le cache JSON corrigé ; REVIEW, QA et SECURITY doivent être rejoués sur ce nouveau hash avant INTEGRATION/E2E.

Décision correctifs DEV finaux Sprint 7 — S7-B 2026-08-23 : les deux P1 SECURITY/PERFORMANCE du candidat `0aec6303…` sont corrigés dans un nouveau candidat à revalider. La consultation du journal d’audit conserve les métadonnées des actions Finance, mais remplace `before`/`after` par `null` et retire tout montant/snapshot lorsqu’un acteur possède `audit.read` sans `finance.read`; un test négatif vérifie l’absence de `costSnapshot`, `costUnitMinor`, `amountMinor` et `totalMinor`, tandis qu’un administrateur Finance conserve le détail complet. Le cache validé de la persistance stocke désormais la représentation JSON immuable déjà écrite et restitue chaque transaction par `JSON.parse`, ce qui maintient l’isolation des mutations tout en supprimant le surcoût instable de `structuredClone` sur le dataset 10k. Preuves DEV fraîches Node v26.6.0 : Réalisations+Finance 24/24, suite complète 295/295, lint/build/diff-check PASS ; deux campagnes isolées `benchmark:actuals` donnent respectivement confirmation/correction p95 `146,28/146,30 ms` et `146,98/144,80 ms`, nettement sous le seuil `<250 ms`. Re-REVIEW, re-QA et revalidation SECURITY/PERFORMANCE sont requises avant INTEGRATION/E2E S7-B.

Décision re-gates SECURITY/PERFORMANCE Sprint 7 — S7-B 2026-08-23 : **BLOQUÉ — retour DEV** sur HEAD `6bbc224c`, code applicatif `0aec6303`. SECURITY est `REJECTED` avec 1 P1 : `/api/v1/audit` restitue les détails `before/after` Finance à un rôle `audit.read` sans `finance.read`, ce qui peut exposer snapshots et montants internes. PERFORMANCE est `REJECTED` avec 1 P1 : Finance reste conforme (p95 `41,76 ms`) et une première passe Réalisations est conforme, mais une seconde mesure isolée atteint `252,79 ms` sur la confirmation pour un seuil strict `<250 ms`. Les correctifs de scopes mutationnels et de cache/tamper restent fermés. Retour DEV ciblé, puis re-REVIEW et tous gates aval impactés.

Décision re-QA Sprint 7 — S7-B 2026-08-23 : **QA APPROVED** sur HEAD `39cd261621d719618d4bf40fc75e64f840ce111e`, code applicatif exact `0aec6303c9b9f5672be4c512277cfca6a6e99988`, avec 0 P0/0 P1. Preuves fraîches : Finance+Réalisations+Devis 72/72, suite complète 294/294, lint/build/diff-check PASS, benchmark Finance p95 `36,48 ms`. Les créations et changements de cible hors Personne/Site/Projet/Client sont refusés sans état partiel ; falsification externe, cache, historique et rollback restent conformes. Une sonde additionnelle de comptage de snapshots a été interrompue par le sandbox et n'est pas revendiquée ; inspection, suite et benchmark couvrent le comportement incrémental. SECURITY/PERFORMANCE restent à rejouer avant INTEGRATION/E2E.

Décision re-REVIEW Sprint 7 — S7-B 2026-08-23 : **REVIEW APPROVED** sur le commit exact `0aec6303c9b9f5672be4c512277cfca6a6e99988`, avec 0 P0/0 P1 et 3 P2 suivis. Les créations et changements de cible des tarifs Personne, ainsi que les dépenses Projet, sont bornés par l'adhésion, le site réel, le Client et le Projet ; les snapshots planifiés incrémentaux et le cache lié à la signature physique du fichier préservent atomicité, falsification et rollback. Preuves reviewer fraîches : 24/24 ciblés, 294/294 complets, syntaxe et diff-check PASS. QA et SECURITY/PERFORMANCE doivent encore être rejoués indépendamment sur ce commit avant INTEGRATION/E2E.

Décision correctifs DEV Sprint 7 — S7-B 2026-08-23 : les deux P1 REVIEW/SECURITY et le P1 PERFORMANCE du candidat `b42ea165…` sont corrigés dans un nouveau candidat DEV à revalider. Les créations et changements de cible des tarifs Personne exigent désormais une adhésion active autorisée ; les dépenses vérifient le site réel du Projet ainsi que le Client, même lorsque `siteId` est absent ou nul. Les coûts planifiés sont figés uniquement pour les versions de Réservation réellement créées ou modifiées ; une mutation de statut conserve le snapshot antérieur lorsque la base de calcul est inchangée. Le dépôt JSON reste atomique et compact, et son cache validé est lié à l'inode, la taille, `mtime` et `ctime` : toute écriture externe force une revalidation complète des migrations et digests. Preuves DEV fraîches Node v26.6.0 : Réalisations+Finance 23/23, suite complète 294/294, lint/build/diff-check PASS ; benchmark Finance 250 ressources/10 000 réservations/2 000 documents/réalisés/coûts p95 `38,75 ms` ; benchmark Réalisations 10 011 réservations/2 500 réalisés p95 lecture `≤128,63 ms`, confirmation `237,89 ms`, correction `238,13 ms` (seuils respectés). Retour requis en re-REVIEW, re-QA et revalidation SECURITY/PERFORMANCE ; INTEGRATION/E2E S7-B restent bloqués jusque-là.

Décision re-gates Sprint 7 — S7-B 2026-08-23 : **BLOQUÉ — retour DEV** sur le candidat applicatif exact `b42ea165ed32eeebae0b3f9f2080520bf946d4d8`. QA est `APPROVED` (71/71 ciblés, 293/293 complets, lint/build/OpenAPI/diff-check PASS). REVIEW est `CHANGES REQUIRED` avec 2 P1 : une mutation de tarif `person` peut viser une personne hors site faute de contrôle d'adhésion, et une dépense peut viser un Projet/Client hors périmètre lorsque `siteId` est absent. SECURITY confirme le P1 de mutations hors site. PERFORMANCE est `REJECTED` avec 1 P1 : le benchmark Marges est conforme (p95 `36,49 ms`), mais le premier enregistrement d'un réalisé déclenche un backfill global dans `atomicWrite` (confirmation p95 `8 937 ms`, correction p95 `284,22 ms`, seuil `250 ms`). Les P1 historiques de confidentialité des snapshots, de lecture des quatre scopes et de définition/provenance des marges sont fermés. Retour DEV ciblé requis, puis re-REVIEW, re-QA et revalidation SECURITY/PERFORMANCE ; INTEGRATION/E2E S7-B restent bloqués.

Décision correctifs DEV Sprint 7 — S7-B 2026-08-23 : les P1 REVIEW/SECURITY/PERFORMANCE du candidat `59ad25a…` sont corrigés dans un nouveau candidat DEV à revalider. Les coûts planifiés sont désormais figés par version de réservation dans une collection privée `plannedCostSnapshots`, y compris lorsque le tarif était indisponible, sans exposer ces données sur la Réservation. Les révisions de réalisé ne restituent leur `costSnapshot` qu'avec `finance.read`. La consultation des coûts applique les scopes Site/Entité aux ressources, personnes et catégories ; les marges filtrent aussi Client, Devis, ressource et prestation avant agrégation. La réponse Finance publie une définition versionnée, la fraîcheur, les compteurs de sources autorisées et un drill-down borné, désormais rendu dans l'interface. Les index de tarifs, révisions et snapshots suppriment les scans répétés. Preuves DEV fraîches Node v26.6.0 : Commercial+Réalisations+Finance 71/71, suite complète 293/293, lint/build/diff-check PASS ; benchmark `npm run benchmark:finance` sur 250 ressources, 10 000 réservations, 2 000 documents commerciaux, 2 000 réalisés et 2 000 coûts Projet : p95 `32,22 ms` (< 300 ms). Le candidat reste non approuvé jusqu'aux re-REVIEW, re-QA et revalidations SECURITY/PERFORMANCE indépendantes ; INTEGRATION/E2E S7-B restent bloqués.

Décision gates Sprint 7 — S7-B 2026-08-23 : **BLOQUÉ — retour DEV** sur le candidat applicatif exact `59ad25a339112dc4faa7df556e43aace6c1cb1ae`. QA est `APPROVED` (20/20 ciblés, 291/291 complets, lint/build/OpenAPI/diff-check PASS). REVIEW reste `CHANGES REQUIRED` avec 3 P1 : coûts planifiés recalculés depuis un tarif mutable sans snapshot historique, liste des tarifs internes insuffisamment bornée par site, et marges sans filtrage fin ni définition/provenance/drill-down. SECURITY est `REJECTED` avec 1 P1 : le `costSnapshot` interne est exposé aux rôles `actual.read` dépourvus de `finance.read`. PERFORMANCE est `BLOCKED` avec 1 P1 de preuve : aucun benchmark Finance sur le dataset contractuel et plusieurs scans à optimiser. Retour DEV ciblé requis, puis re-REVIEW, re-QA et revalidation SECURITY/PERFORMANCE ; INTEGRATION/E2E S7-B restent bloqués.

Décision DEV Sprint 7 — S7-B 2026-08-23 : le moteur de coûts historiques et de marges est implémenté sur un candidat DEV non encore approuvé. Une migration additive distincte `sprint-7-finance-costs-v1` crée les coûts internes datés, dépenses Projet, révisions append-only et marqueurs d’idempotence, avec sauvegarde privée `0600`, validation des références/intégrité et rollback byte-exact précédé d’un export obligatoire. La résolution applique ressource puis catégorie sur intervalles semi-ouverts sans chevauchement ; les nouvelles révisions de réalisé portent un digest V3 incluant l’instantané du coût résolu, afin qu’une modification tarifaire future ne réécrive jamais l’historique. Les dépenses confirmées sont versionnées, l’annulation est terminale, et les marges Devis/planifiées/réelles sont calculées en entiers, uniquement sur le sous-ensemble autorisé par `finance.read`, les scopes Projet/Site/Entité et les Devis visibles. La page autonome « Finance & marges » permet la saisie simple des coûts et dépenses, affiche les périodes, dépenses et indicateurs, avec mutations réservées à `finance.cost.manage`. OpenAPI expose les six opérations S7-B et les nouveaux schémas. Preuves DEV fraîches Node v26.6.0 : ciblés Réalisations+Finance 20/20, suite complète 291/291, lint/build/diff-check PASS. S7-B retourne maintenant aux gates REVIEW, QA, SECURITY/PERFORMANCE avant INTEGRATION/E2E ; S7-C/D et G7 restent en attente.

Décision INTEGRATION/E2E Sprint 7 — S7-A 2026-08-23 : le candidat `27ad4965` a été rejoué dans un serveur isolé sur le port 8213 avec persistance dédiée. Depuis l’interface Réalisations, l’administrateur a confirmé une session (file 3→2, historique 0→1), puis créé une correction motivée (révision 2, quantité 1,5, écart 0,5). Le résultat a persisté après rechargement, reconnexion et redémarrage complet du serveur. Le lecteur voit l’historique mais aucun bouton Confirmer/Corriger. La console navigateur est restée sans erreur ; le serveur a été arrêté et les fichiers temporaires supprimés. **S7-A est intégré et validé E2E** ; la prochaine étape est S7-B « coûts historiques », sans déclarer G7 tant que S7-B/C/D ne sont pas terminés et gated.

Décision gates Sprint 7 — S7-A 2026-08-23 : le candidat `27ad4965` est **APPROVED** en REVIEW, QA, SECURITY et PERFORMANCE, sans P0/P1. L’unité d’un réalisé est celle du snapshot planifié ; la lecture par réservation ne restitue que sa version courante ; `quote.read` et le scope de chaque devis principal ou complémentaire sont revalidés avant réconciliation ; les digests V2 couvrent société, auteur et horodatages. Les révisions sont indexées en O(A+R) et la pagination précède la projection. Preuves finales : ciblés 14/14, suite 284/284, lint/build/diff-check PASS ; benchmark indépendant 10 011 réservations/2 500 réalisés sous les seuils (p95 lectures ≤ 281,85 ms, écritures ≤ 236,07 ms). Les P2 de couverture/mesure restent suivis. S7-A peut passer INTEGRATION/E2E ; S7-B, S7-C et S7-D restent nécessaires avant G7.

Décision SPEC Sprint 7 2026-08-23 : après la release locale `0.3.0-rc1`, le Sprint 7 « Réalisé & Finance Engine » est lancé depuis les quinze stories V1 exactes `US-077` à `US-080` et `US-083` à `US-093` (127 points). La spécification `docs/specifications/sprint-7-actuals-finance-engine.md` fixe un registre de réalisé append-only distinct du planifié, la réconciliation déterministe réalisé/facturable, les coûts historiques, les marges, le backlog, le forecast et l’occupation/rentabilité. Les factures, encaissements, dashboards finaux et exports restent aux Sprints 8/évolutions ultérieures. Premier incrément : S7-A, « réservation terminée → à confirmer → réalisé validé/corrigé », avec permissions, audit, idempotence, OpenAPI, migration/rollback et tests négatifs. G7 reste **BLOQUÉ** jusqu’à la réconciliation « prévu/planifié/réalisé/facturable », aux quatre incréments DEV et aux gates indépendants.

Décision RELEASE G6 2026-08-23 : **APPROVED — Planify `0.3.0-rc1`**. Tous les gates SPEC, DEV, REVIEW, QA, SECURITY/PERFORMANCE, INTEGRATION et E2E portent sur le candidat applicatif exact `1eab12023a44d65bb9d63dc3bfeba6e04399826f`, complété uniquement par les documents d’intégration/E2E et les métadonnées de release. La release livre PlanyBot et l’import contrôlé Excel/CSV/PDF texte selon la règle « propose, confirme, exécute ». Preuves finales : ciblés PlanyBot/Devis/API 104/104, suite complète 270/270, lint/build/diff-check PASS, démarrage local et rollback Sprint 6 documentés. P2 non bloquant conservé : double parcours de lecture XLSX générique réduisant la capacité pratique.

Décision E2E G6 2026-08-23 : **APPROVED — 0 P0 / 0 P1** sur le candidat applicatif `1eab12023a44d65bb9d63dc3bfeba6e04399826f` et le commit d’intégration `a9a75c9`. La recette navigateur isolée a vérifié connexion, panneau PlanyBot, prévisualisation complète, refus sans création, confirmation humaine créant une seule réservation, mise à jour temps réel, rôle lecteur sans mutation et persistance après redémarrage. Les négatifs conflit/override, idempotence, import Excel/CSV/PDF, clarification versionnée et réduction de scopes ont été rejoués dans les tests ciblés. Preuves fraîches : ciblés PlanyBot/Devis/API 104/104, suite complète 270/270, lint/build/diff-check PASS. Rapport : `docs/e2e-report.md`. Étape suivante : RELEASE sur le même candidat ; le P2 de double lecture XLSX reste suivi.

Décision INTEGRATION G6 2026-08-23 : **APPROVED — 0 P0 / 0 P1** sur le candidat applicatif exact `1eab12023a44d65bb9d63dc3bfeba6e04399826f` et la baseline documentaire `6e0b31f`. Le parcours navigateur local isolé a confirmé : proposition PlanyBot sans réservation silencieuse, confirmation humaine explicite, création d’une réservation unique visible dans le Planning, actualisation SSE avec relecture API, chaîne d’audit complète et persistance après arrêt/redémarrage. Preuves fraîches : suite complète 270/270, lint/build/diff-check PASS, aucune erreur navigateur. Le serveur, l’onglet et les données temporaires ont été nettoyés. Rapport : `docs/integration-report.md`. Étape suivante : gate E2E, puis RELEASE sur le même candidat.

Décision gates indépendants G6 2026-08-23 : **APPROVED — 0 P0 / 0 P1** sur le commit exact `1eab12023a44d65bb9d63dc3bfeba6e04399826f`. REVIEW, QA, SECURITY et PERFORMANCE approuvent le même candidat. Le scénario négatif multisite crée un résumé Paris+Boulogne, conserve l’accès au Projet mais retire Boulogne, puis obtient `404` au rejeu et à la lecture de l’historique. Les recommandations revalident `quote.read`, les agrégats revalident sites et scopes d’entité, et l’OpenAPI résout tous ses schémas/paramètres. Preuves fraîches : PlanyBot 14/14, ciblés QA 74/74, suite complète 270/270, lint/build/diff-check/OpenAPI PASS. Performance sur 10 000 éléments : p95 `69,36 ms`, provenance `438 octets`. Un P2 non bloquant reste suivi : double parcours de lecture XLSX générique réduisant la capacité pratique. Étapes suivantes : INTEGRATION puis E2E sur ce candidat avant toute RELEASE.

Décision DEV `SEC-G6-04` 2026-08-23 : les réponses PlanyBot qui agrègent des données de Planning ajoutent désormais à leur provenance compacte une empreinte déterministe des sites autorisés, en plus des gardes Réservation/Ressource. Un test construit un résumé Projet Paris+Boulogne, retire ensuite Boulogne et vérifie `404` au rejeu comme à la lecture de l’historique, sans recopier les identifiants sources. PlanyBot 14/14 et syntaxe serveur/tests PASS. Suite complète et gates impactés à rejouer avant déblocage G6.

Décision gates G6 sur `b25c61d` 2026-08-23 : REVIEW et QA sont `APPROVED` (0 P0/P1 ; suite 270/270 et OpenAPI sémantique PASS). PERFORMANCE est `APPROVED` : pour 10 000 réservations, la provenance compacte mesure 270 à 374 octets et le p95 complet résumé/snapshot/validation/merge reste entre `17,21 ms` et `68,63 ms`. SECURITY reste `REJECTED` avec le seul P1 `SEC-G6-04` : un résumé multi-site sans site explicite n’empreinte pas les sites sources ; retirer ensuite Boulogne peut laisser rejouer l’ancien agrégat Paris+Boulogne. Retour DEV ciblé sur la provenance site, puis revalidation des gates impactés ; G6 reste bloqué.

Décision DEV ultime G6 2026-08-23 : les trois P1 du candidat `14c1268…` sont corrigés. Les réponses PlanyBot enregistrent désormais une provenance `schemaVersion: 3` compacte : permissions réellement consommées et empreintes des scopes Réservation/Ressource, sans recopier les milliers d’identifiants d’un Projet. Les recommandations utilisant une préférence client exigent encore `quote.read` au rejeu et à la lecture ; toute réduction des scopes mobilisés invalide aussi les agrégats historiques. Les anciennes provenances non démontrables échouent fermées. Le schéma OpenAPI `ReservationAllocation` est défini et les 228 références des 46 chemins sont résolues. Preuves DEV fraîches Node v26.6.0 : PlanyBot 14/14, suite complète 270/270, lint, build et diff-check PASS ; provenance du résumé inférieure à 2 000 caractères dans le test représentatif. Nouveau candidat à figer puis à soumettre aux gates REVIEW, QA, SECURITY et PERFORMANCE indépendants ; G6 reste bloqué jusque-là.

Décision re-gates G6 2026-08-23 : **BLOQUÉ — retour DEV** sur le commit `14c1268cfcdcbefdcee8bf7a6be10419ef307f14`. REVIEW relève un P1 de provenance résiduel sur les recommandations PlanyBot (`clientPreference`/`continuity`). QA relève un P1 de contrat OpenAPI : le schéma référencé `ReservationAllocation` est absent. SECURITY et PERFORMANCE relèvent un P1 commun : la provenance d’un résumé Projet peut recopier des milliers d’identifiants Réservation/Ressource et les revalider en O(n²), avec une mesure fraîche d’environ `399,89 ms` pour 10 000 identifiants et `78 910 octets` par copie avant persistance. Retour DEV ciblé requis : permissions et scopes réellement mobilisés, provenance compacte et bornée, résolution complète des références OpenAPI, puis relecture indépendante de tous les gates G6.

Décision DEV correctifs finaux G6 2026-08-23 : les deux P1 de la re-REVIEW `6381cbe…` sont corrigés. La provenance PlanyBot porte désormais une version de schéma, les permissions effectivement utilisées et les identifiants Réservation/Ressource qui produisent les agrégats ; toute donnée historique sans provenance démontrable échoue fermée. Les tests retirent successivement `quote.read`, puis les scopes Réservation/Ressource en conservant le Projet, et obtiennent `404` au rejeu comme à la lecture. Les quatre opérations OpenAPI G6 déclarent maintenant `quoteId` en paramètre de chemin requis et un contrôle sémantique parcourt tous les templates. Preuves DEV fraîches Node v26.6.0 : PlanyBot 14/14, suite complète 270/270, lint, build, OpenAPI 46/46 chemins valides et diff-check PASS. Ce nouveau candidat invalide les anciens verdicts sur le code modifié ; re-REVIEW, re-QA et revalidation d’impact SECURITY/PERFORMANCE sont requises avant de débloquer G6.

Décision re-gates G6 2026-08-23 : **BLOQUÉ — retour DEV** sur le commit `6381cbeb7020d57ac21e2086a3d5475d9d675325`. SECURITY et PERFORMANCE sont `APPROVED` (parseurs bornés/asynchrones, tests ciblés 62/62, seuils Planning respectés). REVIEW reste `CHANGES REQUIRED` avec 2 P1 : revalidation incomplète de `quote.read` et des sources Réservation/Ressource ayant contribué aux agrégats PlanyBot ; paramètre de chemin `quoteId` absent sur quatre opérations OpenAPI. QA est `NOT APPROVED` avec un P1 de preuve, la campagne complète indépendante ayant été interrompue après 73/73 tests ciblés verts. Retour DEV ciblé, puis re-REVIEW, re-QA et revalidation SECURITY/PERFORMANCE d’impact requises ; gates aval toujours bloqués.

Décision DEV correctifs G6 2026-08-23 : les quatre P1 REVIEW/SECURITY/PERFORMANCE du candidat `cdc475c…` sont corrigés dans un nouveau candidat DEV à revalider. Les réponses et historiques PlanyBot mémorisent désormais les Projets, sites, Devis, imports, ressources, réservations et personnes réellement exposés, puis revalident ces autorités à chaque rejeu et lecture. L’import direct Excel → Devis impose la même clarification humaine versionnée qu’avant une conversion Planning et refuse toute ligne ambiguë, non reconnue ou altérée. Les parseurs XLSX/PDF/CSV sont asynchrones et bornés par entrée, archive, feuilles, lignes, colonnes, cellules et texte décompressé agrégé. L’OpenAPI expose les routes G6 manquantes de conversation, analyse, application et conversion. Preuves DEV fraîches Node v26.6.0 : Clients + PlanyBot 24/24, Devis 49/49, suite complète 269/269, lint, build, syntaxe, OpenAPI YAML 3.1 et diff-check PASS. G6 reste **BLOQUÉ** jusqu’aux revalidations indépendantes REVIEW, QA, SECURITY et PERFORMANCE sur ce nouveau candidat ; INTEGRATION, E2E et RELEASE restent en attente.

Décision G6 2026-08-23 : **BLOQUÉ — retour DEV** sur le candidat exact `cdc475c9ff015531e662327dbdc9d7c2e82f6aa8`. QA est `APPROVED` (0 P0/P1 ; ciblés 102/102, suite 267/267, lint/build/diff-check PASS), mais REVIEW est `NOT APPROVED` avec 4 P1 et SECURITY/PERFORMANCE sont `REJECTED`. Les corrections obligatoires sont : revalider au rejeu et à la lecture toutes les entités réellement exposées par PlanyBot après réduction des scopes ; imposer le même parcours de clarification versionnée à l’import direct Excel → Devis ; borner globalement et rendre non bloquante l’analyse XLSX/PDF ; compléter l’OpenAPI G6 (conversation, analyse, application et prévisualisation). Les gates indépendants concernés et tous les gates aval devront être rejoués sur le nouveau candidat. INTEGRATION, E2E et RELEASE restent bloqués.

Décision DEV Sprint 6 S6-D — clarifications Excel versionnées 2026-08-23 : l’analyse locale Excel/CSV/PDF conserve désormais sa sortie originale et enregistre chaque correction humaine dans une révision immuable, numérotée, horodatée, signée par digest et auditée. Une ligne `ambiguous` ou `unmatched` ne peut plus atteindre la prévisualisation Planning avant confirmation explicite de la ligne du devis, de la ressource, des dates, horaires et d’un motif. Le rejeu exact est idempotent ; une sélection divergente ou fondée sur une ancienne correction est refusée. L’interface PlanyBot demande le motif, enregistre la révision avant la vérification des disponibilités et n’effectue toujours aucune réservation silencieuse. L’OpenAPI expose la commande de révision. Preuves DEV fraîches Node v26.6.0 : Devis 48/48, suite complète 266/266, lint, build, syntaxe et diff-check PASS. S6-A/B/C/D sont intégrés en DEV ; REVIEW, QA, SECURITY/PERFORMANCE, INTEGRATION et E2E G6 restent requis sur un candidat figé.

Décision DEV Sprint 6 S6-B/C — classement explicable finalisé 2026-08-23 : les réponses Disponibilité et Préparation de réservation exposent un classement stable des ressources autorisées. L’autorité filtre disponibilité, compatibilité et scopes avant de classer par continuité Projet, préférence client structurée issue d’une grille active, site du Projet, coût interne autorisé puis nom/identifiant stable. `quote.read` est requis pour exploiter une préférence client ; `finance.read` est requis pour utiliser le coût comme départage et sa valeur n’est jamais exposée. Chaque candidat donne les raisons réellement appliquées. La préparation reste une proposition sans mutation jusqu’à confirmation humaine. Preuves DEV fraîches Node v26.6.0 : PlanyBot 12/12, suite complète 267/267, lint, build et diff-check PASS. S6-A/B/C/D sont prêts à être figés pour les gates G6 indépendants ; aucun verdict indépendant n’est revendiqué par l’auteur.

Décision DEV Sprint 6 S6-A 2026-08-22 : le cycle persistant « PlanyBot propose, l’humain confirme, le moteur Réservation exécute » est intégré sur un candidat `0.3.0-dev` non encore approuvé. Une demande prépare une proposition privée avec payload canonique, versions sources, digest, prévisualisation et expiration, sans créer de réservation. La confirmation exige `planning.write`, une clé d’idempotence stable, revalide droits/scopes/Projet/ressource/version/disponibilité et appelle la commande Réservation existante ; le refus ne produit aucune mutation métier. Les replays divergents sont refusés et l’audit relie préparation, décision et résultat. La migration additive `sprint-6-planybot-proposals-v1` crée une sauvegarde privée `0600`, un marqueur d’intégrité et un rollback byte-exact précédé d’un export de reprise. L’OpenAPI et le panneau accessible sont alignés. Preuves fraîches Node v26.6.0 : PlanyBot + migration 12/12, suite complète 265/265, lint/build/diff-check PASS. S6-A est intégré en DEV ; S6-B/C/D (dialogue enrichi, classement explicable, analyse Excel et clarifications versionnées) restent à réaliser avant REVIEW G6.

Décision SPEC Sprint 6 2026-08-22 : après la release locale `0.2.0-rc2`, le Sprint 6 « PlanyBot & import Excel » est lancé depuis les sept stories V1 exactes `US-057` à `US-060` et `US-062` à `US-064` (68 points). `US-061` reste V2. La spécification `docs/specifications/sprint-6-planybot-excel.md` fixe le cycle non contournable « l’IA propose, l’humain confirme, le moteur exécute », le classement explicable des ressources, les clarifications Excel versionnées, l’audit minimisé et l’absence de SaaS/réseau. L’implémentation PlanyBot existante est une base partielle à revalider, pas une preuve G6. Premier incrément : S6-A contrats, propositions confirmables, audit, OpenAPI, migration/rollback et tests négatifs. G6 reste bloqué jusqu’au parcours E2E preview → confirmation → exécution sans mutation silencieuse et aux gates indépendants.

Décision release `0.2.0-rc2` 2026-08-22 : **APPROVED — 0 P0 / 0 P1**. Le P1 QA de la page Équipe est fermé par un annuaire opérationnel minimal séparé de la gouvernance des accès. Le contrôle navigateur réel avec le rôle Planificateur affiche Camille Martin et Alex Bernard, renseigne les deux sélecteurs Personne et masque « Gérer les accès ». REVIEW, QA, SECURITY et PERFORMANCE approuvent le candidat exact ; preuves fraîches : API 41/41, Organisation 34/34, Planning 43/43, suite complète 262/262, lint/build/diff-check PASS. La publication ou le déploiement distant ne sont pas demandés.

Décision retour DEV après recette visuelle 2026-08-22 : le contrôle navigateur du candidat `0.2.0-rc1` a détecté que l’entrée « Équipe » tombait sur la page générique Paramètres, puis restait verrouillée lorsqu’elle était reliée directement à l’onboarding O3 d’une organisation historique. La route `#team` ouvre désormais une page opérationnelle autonome : annuaire actif, compteurs, compétences et indisponibilités, avec les permissions `planning.read`/`planning.write` et les API/audits existants. Le P1 QA d’intégration est fermé par `GET /api/v1/personnel-directory` : un planificateur obtient uniquement `id`, `displayName`, `jobTitle` et le site par défaut autorisé, sans e-mail, rôle, identifiant utilisateur ni scope de gouvernance ; l’accès « Gérer les accès » reste réservé à `membership.read`. Le test négatif confirme aussi qu’un planificateur Paris ne voit aucun membre rattaché par défaut à Boulogne. Contrôle navigateur admin initial : entrée Équipe active, 3 membres Northlight affichés, formulaires Compétence et Indisponibilité utilisables, aucune dépendance au statut O3. Preuves DEV fraîches : API 41/41, Organisation 34/34, suite complète 262/262, syntaxe PASS. Le tag local `v0.2.0-rc1` reste l’ancienne empreinte et ne couvre pas ce correctif. Le nouvel état est `0.2.0-rc2-dev` jusqu’aux revalidations indépendantes frontend et à la recette visuelle planner.

Décision SPEC Sprint 5 2026-08-21 : G4 étant franchi, le Sprint 5 « Ressources avancées & temps réel » est lancé depuis les huit stories exactes `US-068` et `US-070` à `US-076` du Backlog V1. La spécification `docs/specifications/sprint-5-advanced-resources-realtime.md` conserve SSE comme transport local RC1 et sépare clairement présence/verrou éphémère de l'autorité de concurrence par `version`. Les tranches prévues sont S5-A temps réel fiable, S5-B double option/ressource générique, S5-C compétences/indisponibilités et S5-D preuve à trois planificateurs. Le Gate G5 reste bloqué jusqu'à la démonstration « trois opérateurs simultanés, aucune perte ni écrasement silencieux » et aux gates indépendants.

Décision DEV Sprint 5 S5-A1 2026-08-21 : le premier contrat de présence multi-opérateur est intégré côté serveur sans migration de données. Une session autorisée acquiert ou renouvelle pendant 20 secondes l'intention `editing`, `moving` ou `resizing`; une autre session reçoit `423 RESERVATION_LOCKED`. La liste et les événements restent filtrés par société/site/projet/entités, la version courante est exigée et la déconnexion libère les présences de la session. La présence est éphémère et ne remplace pas le verrouillage optimiste. OpenAPI est aligné. Preuves fraîches Node v26.6.0 : API 33/33, suite complète 246/246, syntaxe et diff-check PASS. Le lot reste en DEV : branchement frontend/heartbeat, expiration observée et preuve à trois sessions sont encore requis avant REVIEW G5.

Décision DEV Sprint 5 S5-A2 2026-08-21 : le consommateur frontend de présence est intégré. L'ouverture d'une réservation acquiert le verrou court avec sa `version`, le renouvelle toutes les 8 secondes et le libère à la fermeture du formulaire ou à la sortie du Planning. Les événements SSE de présence ne rechargent que les présences ; ils n'activent ni l'indicateur d'autosave ni une mutation métier. Une carte tenue par un autre opérateur affiche son nom et l'intention, porte `aria-disabled`, n'est plus déplaçable et conserve une information non fondée sur la couleur. Smoke navigateur local isolé : Fernando ouvre `reservation_2`, la présence est visible par Camille ; après une présence Camille, Fernando voit `Camille Martin · déplacement`, `draggable=false`, puis la liste redevient vide après fermeture. Aucune erreur console ; serveur et onglet temporaires arrêtés. Preuves fraîches : Planning 40/40, API 33/33, suite complète 248/248, lint/build/diff-check PASS. Candidat DEV : serveur `e3cd3241…`, frontend `5908cc07…`, CSS `5663dab7…`, API `b426479e…`, Planning `d9e74e82…`, OpenAPI `3775031a…`. S5-A est fonctionnellement intégré ; G5 reste bloqué par S5-B/S5-C et la preuve E2E à trois opérateurs de S5-D, puis par les gates indépendants.

Décision DEV Sprint 5 S5-B1 2026-08-21 : les contrats domaine/API de double option et ressource générique sont intégrés. Deux options ne peuvent ignorer leur conflit que si elles partagent un `optionGroupId` complet ; la priorité la plus haute est confirmée atomiquement, une confirmation secondaire prématurée répond `409 OPTION_PRIORITY_BLOCKED`, et l'option perdante reste visible avec une décision structurée et le gagnant référencé. Une allocation `generic:true` référence une catégorie active du site et réserve sa capacité agrégée ; l'affectation ultérieure par `/generic-assignments` exige une ressource réelle active, compatible, disponible et autorisée, conserve `genericAllocationId`, contrôle la version et rejoue idempotemment sans fuite de scope. La migration additive `sprint-5-advanced-resources-v1` ne réécrit aucune réservation historique ; elle crée une sauvegarde byte-exacte `0600`, vérifie les invariants à la relecture et dispose d'un rollback avec export obligatoire documenté. OpenAPI et README sont alignés. Preuves fraîches Node v26.6.0 : API 35/35, suite complète 251/251, lint/build/syntaxe/diff-check PASS. Candidat : serveur `3293935e…`, API `a9db823f…`, OpenAPI `2035e22c…`, spec `119638a9…`. Le lot S5-B reste partiel tant que les libellés/commandes UI double option et salle à définir ne sont pas intégrés ; S5-C et S5-D restent à faire.

Décision DEV Sprint 5 S5-B2 2026-08-21 : l'interface Options et ressources génériques est intégrée. Le formulaire distingue « Salle précise » et « Salle à définir », filtre les catégories actives du site et n'affiche les champs groupe/priorité/échéance que pour le statut Option. Une allocation générique ne produit aucune cellule fictive : elle apparaît dans une file accessible « Salles à définir », avec choix d'une ressource réelle compatible et commande d'affectation idempotente. Les cartes et la file annoncent textuellement la double option, sa priorité et les décisions retenue/perdue. Smoke navigateur isolé sur port 8209 : champs conditionnels conformes ; création de `Test S5 — salle à définir` ; première salle occupée refusée sans perte ; affectation à `Poste Remote AVID 10` réussie ; file retirée et aucune erreur console. Données uniquement dans `/private/tmp/planify-sprint5-preview.json`. Preuves fraîches Node v26.6.0 : Planning 42/42, API 35/35, suite complète 253/253, lint/build/syntaxe/diff-check PASS. Candidat DEV : serveur `3293935e…`, frontend `d2fe75c9…`, CSS `2d09f0b9…`, shell `edada446…`, API `a9db823f…`, Planning `15f0e3b3…`, OpenAPI `2035e22c…`. S5-B est intégré en DEV ; S5-C personnel/indisponibilités et S5-D trois planificateurs restent requis avant G5.

Décision DEV Sprint 5 S5-C/D 2026-08-21 : les compétences et indisponibilités structurées sont intégrées avec interface O3, RBAC/scopes, audit/SSE, PlanyBot sans coordonnées personnelles et mutations entièrement idempotentes. Le marqueur `sprint-5-personnel-v1` dispose d'une sauvegarde privée byte-exacte et d'un rollback autonome exigeant un export `0600`. Le scénario S5-D ouvre trois sessions simultanées : les deux observateurs reçoivent les invalidations Personnel et Réservation par SSE, une session concurrente reçoit `423`, une version obsolète reçoit `409` sans écrasement, la déconnexion libère le verrou, puis la donnée et le SSE sont vérifiés après redémarrage du serveur. Le contrôle navigateur isolé confirme le Planning chargé, la grille et ses indicateurs accessibles ; le panneau O3 reste correctement verrouillé tant que les prérequis d'identité du seed ne sont pas validés. Le contrôle de capacité des doubles options utilise un index linéaire plutôt qu'une recherche quadratique et le SSE ne relit la base qu'en présence d'abonnés. Preuves fraîches Node v26.6.0 : API 37/37, Planning 43/43, migration Personnel 1/1, temps réel trois sessions 1/1, suite complète 257/257, lint/build/syntaxe/diff-check PASS. Benchmark HTTP 250 ressources/10 000 réservations : lecture p95 `115,95 ms`, conflit `165,36 ms`, écriture `204,85 ms`, lot de 100 `249,85 ms`, rejeu `183,56 ms`, tous conformes aux seuils. Empreintes : serveur `54ec6fd5…`, frontend `400d3e04…`, CSS `4016e6d8…`, API `080a40c8…`, Planning `f2827562…`, test temps réel `c7c7a0ea…`, test rollback `d32231df…`, OpenAPI `a588ec9e…`. Le DEV Sprint 5 est intégré ; G5 reste bloqué jusqu'aux verdicts indépendants REVIEW, QA, SECURITY et PERFORMANCE sur ce candidat exact.

Décision Gates G5 2026-08-21 : **BLOQUÉ — retour DEV requis** sur le candidat `server.js 54ec6fd5…`, `app.js 400d3e04…`, `planning.css 4016e6d8…`. QA est `APPROVED` (257/257) et PERFORMANCE est `APPROVED` pour les chemins API (lecture `116,62 ms`, conflit `167,15 ms`, écriture `207,24 ms` p95). REVIEW est `CHANGES REQUESTED` avec 4 P1 : arbitrage collatéral d'options simples sans groupe, permissions SSE Réservation/Ressource non explicites, gestes clavier move/resize sans présence, et libération logout/TTL non notifiée. SECURITY est `REJECTED` avec 3 P1, dont le contrôle SSE partagé avec REVIEW : indisponibilités personnel inter-site insuffisamment filtrées, RBAC SSE incomplet, et connexions SSE non bornées par session. Le benchmark batch100 frais atteint `250,10 ms` et reste suivi en P2 avec la charge SSE/UI. Condition de déblocage : corriger les six risques P1 distincts, ajouter leurs tests négatifs, puis rejouer REVIEW, QA, SECURITY et PERFORMANCE impactés sur un nouveau candidat figé.

Décision V1 2026-08-19 : l'Ordre de lancement et le Backlog V1 placés dans `docs/specifications/` deviennent le référentiel d'exécution. Le Gate G0 est franchi. Le Sprint 1 est lancé le 2026-08-20 sur les douze stories `US-007` à `US-016`, `US-081` et `US-082`. Les écrans métier lourds du Sprint 2 restent suspendus. Le runtime CommonJS/JSON est conservé ; les contrats V1 sont ajoutés sans migration implicite de stack ni de données.

Décision SPEC Sprint 4 2026-08-21 : G3 étant franchi avec Integration/E2E, le Sprint 4 « Planning ↔ Devis » est lancé depuis les quatorze stories exactes `US-038` à `US-042` et `US-048` à `US-056`. La spécification `docs/specifications/sprint-4-planning-quote-continuity.md` ferme le batch atomique, les compensations undo/redo, l’autosave, le moteur canonique vendu/planifié/reste/dépassement et le cycle des Devis complémentaires. Le premier incrément est S4-A Opérations groupées ; G4 reste bloqué jusqu’à la preuve « dépassement = complément exact et transactionnel » et aux gates indépendants.

Décision DEV Sprint 4 S4-A1 2026-08-21 : le collage d’une sélection de cellules n’émet plus une requête par cellule. Le frontend appelle une seule commande `POST /api/v1/reservations/batch`, bornée à 200 actions, idempotente et atomique. Le serveur contrôle chaque source/cible, les scopes, la disponibilité et les liens Devis avant écriture ; une erreur sur n’importe quelle action abandonne le lot complet, sans création partielle. Un rejeu exact restitue les mêmes identifiants et un contenu divergent répond `409`. L’OpenAPI est aligné. Preuves fraîches : API 31/31, Planning 30/30, suite complète 235/235, lint/build/diff-check PASS. Le lot reste en DEV : annulation/rétablissement transactionnels, paint days, autosave et consommation/compléments doivent encore être consolidés avant les gates G4.

Décision DEV Sprint 4 S4-A2/B1 2026-08-21 : l’annulation d’un collage multi-cellules utilise désormais une unique compensation batch atomique et idempotente ; un élément invalide annule toute la compensation. L’indicateur Planning annonce explicitement `Sauvegarde…`, `Synchronisé` ou `Hors connexion` et ne confirme jamais localement une mutation réseau non reçue. Le `QuoteConsumptionEngine` calcule maintenant la base vendue à partir du Devis accepté et de ses compléments acceptés, puis dérive planifié, reste, dépassement et statut ; le contrôle PlanyBot et la synchronisation des compléments utilisent cette même autorité. Le digest exact de la répartition est figé dans chaque validation. Le scénario automatisé `+5 → +3 → 0` démontre la resynchronisation du même complément brouillon puis son annulation logique, sans réécrire le Devis ni le complément acceptés. La copie de cellules conserve désormais la politique temporelle de la source au lieu de promouvoir implicitement un historique legacy. Preuves fraîches : Fondations 15/15, Devis 47/47, API 31/31, syntaxe/diff-check PASS.

Décision DEV Sprint 4 S4-A3 2026-08-21 : le contrat batch accepte maintenant `create` en plus de `duplicate` et `cancel`. Le mode peinture du Planning sélectionne des cellules visibles non contiguës, exige un Devis accepté et une prestation planifiable, prévisualise les cellules sans écriture puis crée le lot dans une transaction unique bornée à 200 actions. Un doublon ou conflit sur une cellule refuse tout le lot ; le rejeu exact est idempotent. Undo annule toutes les cellules peintes par une compensation batch et Redo recrée un nouveau lot. OpenAPI, styles responsive et alternative clavier sont alignés. Preuves fraîches : API 31/31, Planning 32/32, Devis 47/47, suite complète 237/237, lint/build/diff-check PASS. Le contrôle navigateur sur le dataset 250/10 000 n’a créé aucune donnée ; l’accès au mode dépend correctement d’un Devis accepté absent de ce dataset. Le candidat reste en DEV : compensation atomique des moves/resizes groupés, restore d’annulation, E2E navigateur sur un Devis accepté et gates G4 restent requis.

Décision DEV Sprint 4 S4-A4 2026-08-21 : la commande batch couvre désormais les cinq familles canoniques `duplicate`, `move`, `resize`, `cancel`/`restore` et `create`. Déplacement et redimensionnement d’une multi-sélection conservent les identités, durées relatives, exceptions de cellules et liens Projet/Devis ; une erreur de version, de scope ou de disponibilité abandonne toute la transaction. L’annulation mémorise son statut antérieur et peut être rétablie par une compensation serveur contrôlée. Les commandes visibles « Déplacer ici » et « Finir le … » utilisent une cible explicite, sont accessibles au clavier et alimentent une pile Undo/Redo bornée. OpenAPI, audits, SSE et synchronisation du Devis complémentaire sont alignés. Preuves fraîches : API 31/31, Planning 35/35, Devis 47/47, suite complète 240/240, lint/build/diff-check PASS. S4-A, S4-B et S4-C sont fonctionnellement couverts en DEV ; G4 reste bloqué jusqu’au parcours E2E navigateur sur un Devis accepté, aux benchmarks batch 100 et aux gates indépendants REVIEW, QA, SECURITY/PERFORMANCE.

Décision DEV Sprint 4 S4-A5 Performance 2026-08-21 : la première mesure G4 a objectivé un lot de 100 créations à `p95 918,33 ms`, au-dessus de la cible `< 250 ms`. La validation rescannait les 10 000 réservations pour chaque cellule. Le serveur construit désormais une fois par transaction un index de capacité par ressource et le maintient au fil des actions `create`, `duplicate`, `move`, `resize`, `cancel` et `restore`; les conflits entre actions du même lot restent donc détectés sans parcours quadratique. Mesure fraîche sur 250 ressources / 10 000 réservations, 10 lots de 100 : `p50 197,67 ms`, `p95 219,05 ms`, `max 219,05 ms` — **PASS**. Les références HTTP restent conformes (`lecture p95 97,18 ms`, conflit `133,73 ms`, écriture `174,20 ms`). Après optimisation : API 31/31, Planning 35/35, Devis 47/47, suite complète 240/240, lint/build/diff-check PASS. Le candidat DEV doit encore passer l’E2E Devis accepté, puis les gates indépendants G4.

Décision DEV Sprint 4 S4-E2E 2026-08-21 : le parcours navigateur réel a été exécuté sur un fichier temporaire isolé, sans donnée utilisateur. Après connexion et validation O1, un Devis de 5 jours de Montage Avid a été validé, envoyé puis accepté. Le mode peinture a créé 5 cellules en une transaction ; PlanyBot a affiché `5 / 5 jour/salle` et `100 %`. Trois cellules supplémentaires ont synchronisé un unique complément brouillon `DEV-2026-0002` à `+3 jour/salle / 300,00 EUR HT`. Une compensation Undo a retiré exactement ces trois cellules, ramené le contrôle à `5 / 5` et supprimé le dépassement visible ; un rechargement a conservé l'état. Le contrôle a aussi fermé une régression O1 : une organisation active dont le profil fiscal n'est pas encore validé conserve le bouton `Valider O1 et continuer`; une garde automatisée couvre ce cas. Preuves finales : Organisation 33/33, API 31/31, Planning 35/35, Devis 47/47, suite complète 241/241, lint/build/diff-check PASS. Le candidat Sprint 4 est prêt pour les gates G4 indépendants ; aucun gate indépendant n'est encore revendiqué sur ce nouvel état.

Décision retour DEV G4 2026-08-21 : la première campagne indépendante a rendu QA APPROVED mais REVIEW `CHANGES REQUESTED` (4 P1) et SECURITY `NOT APPROVED` (1 P1). Les cinq blocages sont corrigés : le rejeu idempotent batch revalide désormais les scopes actuels ; déplacement et copie reconstruisent les instants depuis le fuseau IANA du site et non depuis des tranches UTC ; Undo/Redo conserve la version attendue, échoue sur divergence, borne toutes les piles à 50 et purge sélection/clipboard/historique au changement de société ; Maj+clic/Maj+Entrée étend une sélection rectangulaire visible ; l'indicateur `Synchronisé` n'apparaît qu'après la fin de toutes les mutations Planning concurrentes. Négatifs ajoutés : réduction de scope avant rejeu => 404, déplacement/copie autour du DST conservent les heures locales, sélection rectangulaire, piles bornées/scopées et compteur autosave. Preuves fraîches : API 32/32, Planning 38/38, suite complète 245/245, lint/build/diff-check PASS. Benchmark isolé 250 ressources / 10 000 réservations : lecture `p95 97,46 ms`, conflit `134,16 ms`, écriture `173,08 ms`, batch100 `227,01 ms`, rejeu `154,94 ms` — PASS. REVIEW, QA et SECURITY/PERFORMANCE doivent maintenant revalider ce nouvel état ; les anciens verdicts ne le couvrent plus.

Décision retour DEV G4 — seconde passe 2026-08-21 : les re-gates ont objectivé deux défauts résiduels. Le rejeu batch contrôlait l'objet courant mais pouvait encore restituer un snapshot historique dont une ancienne ressource était sortie du scope ; il valide désormais séparément l'état courant et chaque résultat historique, y compris société, site, projet, réservation, ressources et exceptions de cellule. Le déplacement/copie vers une heure locale inexistante lors du passage à l'heure d'été est désormais refusé par `422` au lieu d'être normalisé silencieusement. Les tests négatifs couvrent un objet courant visible sur la ressource C avec un résultat historique sur B désormais masquée, ainsi que move et duplicate vers `02:30` inexistante sans mutation partielle. Candidat : serveur `cc9172f1…`, frontend `16dc6c21…`, API `cac073c7…`, Planning `7cb607aa…`; API 32/32 et suite complète 245/245 PASS. REVIEW, QA et SECURITY/PERFORMANCE sont relancés sur ces empreintes exactes avant toute Integration/E2E terminale.

Décision terminale G4 Integration/E2E 2026-08-21 : **APPROVED — 0 P0 / 0 P1** sur le candidat serveur `31f2e713…`, frontend `16dc6c21…`, tests API `05a7d439…`, Planning `7cb607aa…`. L'ultime divergence temporelle est fermée : pour l'heure d'automne répétée `31/10/2027 02:30 Europe/Paris`, move et duplicate choisissent tous deux l'occurrence canonique `earlier`, soit `00:30Z`, identique au frontend ; l'heure de printemps inexistante reste refusée sans mutation. REVIEW, QA, SECURITY et PERFORMANCE rendent tous `APPROVED`, avec 245/245 tests et batch100 `p95 220,15 ms`. Le smoke navigateur isolé sur le fichier temporaire Sprint 4 confirme la sélection rectangulaire de cinq cellules Montage Avid par clic puis Maj+clic, l'indicateur `5 cellule(s) sélectionnée(s)`, la synchronisation, puis la présence des cinq réservations après rechargement et reconnexion. Le serveur temporaire 8208 et l'onglet de contrôle ont été arrêtés proprement. P2 non bloquants suivis : enrichir les réponses d'erreur OpenAPI et ajouter un audit récapitulatif du lot.

Décision DEV G1 2026-08-20 : les six P1 des gates initiaux sont corrigés dans une migration additive `sprint-1-contracts-v2`. Elle complète les contrats Client/Projet, introduit `ResourceCategory`, normalise le périmètre des tarifs, ferme la recherche Client sans permission et fournit un rollback Sprint 1 byte-exact précédé d'un export de reprise obligatoire. Les anciens verdicts G1 ne couvrent pas ce nouveau candidat ; Sprint 2 reste suspendu jusqu'aux revalidations indépendantes.

Décision DEV G1 finale 2026-08-20 : les retours REVIEW/QA du second candidat sont traités. Un Client hérite de la devise de la société et exige une adresse structurée ; un Projet exige explicitement Site et trois responsables actifs et rejette les alias de statut ; les catégories de ressources sont modifiables/archivables sous contrôle de dépendances ; le remplacement d'un Site remappe catégories, ressources, unités et prestations. Le rollback refuse désormais toute absence de marqueur et tout appel sans export, y compris avec un ancien paramètre `allowDataLoss`. Ce nouveau candidat invalide les anciens verdicts jusqu'à revalidation.

Décision DEV G1 intégrité 2026-08-20 : le dernier constat `SEC-G1-03` est corrigé. Le rejeu de la migration refuse désormais un Projet relié à un Client absent, inactif ou d'une autre société, ainsi qu'un responsable absent, inactif ou sans membership actif dans la société. Deux régressions de falsification couvrent Client et responsable. Les verdicts antérieurs à ce diff doivent être rejoués sur le nouveau candidat.

Décision DEV G1 dépendances 2026-08-20 : `SEC-G1-04` est traité en amont des écritures. Un Client référencé par un Projet ne peut plus être désactivé et un membre responsable d'un Projet ne peut plus être suspendu avant réaffectation. Les commandes répondent `409` sans mutation, évitant qu'une écriture autorisée rende ensuite la base illisible.

Décision Gate G1 2026-08-20 : le candidat figé `server.js 32681574…` / `app.js ebd7ab42…` est **APPROVED**. REVIEW, QA, SECURITY et PERFORMANCE concluent chacun à 0 P0/P1 ; la suite complète est verte à 210/210. L'intégration navigateur a créé un Client, un Projet avec Site et trois responsables explicites, une unité interne et une prestation, puis les a retrouvés par la recherche universelle et après redémarrage sur la même persistance. Le dashboard analytique a également été contrôlé. Sprint 2 peut être préparé ; la RELEASE reste distincte et exige encore le figement Git et son gate dédié.

Décision SPEC Sprint 2 2026-08-20 : le Sprint 2 « Commercial et noyau Planning » est lancé depuis l'Ordre et le Backlog V1. Le périmètre couvre US-017 à US-024, US-033/034 et US-065 à US-069 ; les capacités Commercial déjà présentes seront revalidées, tandis que le premier incrément DEV ferme le cycle canonique des statuts de réservation. Le Gate G2 reste conditionné par un Devis accepté exploitable et un viewport virtualisé démontré sur 250 ressources / 10 000 réservations.

Décision DEV Sprint 2 incrément 1 2026-08-20 : US-069 est intégré sur un nouveau candidat non encore approuvé. Les réservations exposent `draft`, `option`, `confirmed`, `completed`, `cancelled`, `unavailable` et `maintenance`; les transitions sont fermées et auditées. Brouillon/réalisée/annulée ne consomment pas la capacité, tandis qu'option/confirmée/indisponible/maintenance la consomment. Les états réalisés et annulés sont terminaux. API, OpenAPI, formulaire et validations frontend sont alignés ; les anciennes valeurs RC1 restent compatibles.

Décision DEV Sprint 2 viewport 2026-08-20 : US-024/033 sont intégrés sur le candidat Sprint 2 non encore approuvé. La matrice virtualise désormais les ressources et les dates avec sur-rendu borné, conserve la longueur logique par espaces avant/après et restaure les positions horizontale/verticale lors du changement de fenêtre. La prévisualisation cible sur 250 ressources / 10 000 réservations a rendu 17 à 18 ressources et 12 à 14 dates, puis changé de fenêtre sur les deux axes sans erreur navigateur ni désalignement visible. Les gates G2 indépendants restent à lancer après la tranche maintenance/Commercial.

Décision DEV Sprint 2 maintenance 2026-08-20 : US-067 est intégré au contrat de réservation canonique. Maintenance et indisponibilité sont hachurées et libellées, filtrables, accessibles et bloquent la capacité ; l'API refuse une réservation concurrente. Les réservations réalisées/annulées restent consultables mais ne peuvent plus être glissées, redimensionnées ou annulées depuis l'interface.

Décision DEV Sprint 2 Commercial 2026-08-20 : US-017 à US-023 sont revalidés sur le candidat consolidé. Le Budget doit être confirmé avant conversion, la source reste conservée et aucune réservation n'est créée automatiquement. Le Devis accepté conserve ses versions et instantanés fiscal/commercial, alimente seul le CA et prépare/crée son Planning de manière idempotente sans altérer HT/TVA/TTC. Les tarifs automatiques suivent projet, client puis catalogue. La suite Devis est verte à 47/47 et la suite complète à 216/216.

Décision retour DEV G2 2026-08-20 : REVIEW a bloqué le premier candidat G2 sur deux P1. Le correctif ciblé interdit désormais toute annulation d'un état `completed` ou `cancelled` et applique `reservationAllowed` dans le cœur des commandes de déplacement/duplication, y compris lorsqu'un identifiant est deviné hors scopes projet/entité. Des tests HTTP négatifs couvrent les trois commandes et l'absence de mutation terminale. Les anciens verdicts G2 ne couvrent plus ce candidat ; re-REVIEW, re-QA et re-SECURITY/PERFORMANCE sont requis.

Décision preuve UI G2 après retour DEV 2026-08-20 : la prévisualisation Performance a rendu 250 ressources et 21 dates logiques avec seulement 17 ressources × 14 dates initialement. Le changement de fenêtre verticale a été observé en 83 ms et l'horizontale en 98 ms, avec 154 cellules après déplacement et aucune erreur console. Cette preuve DEV ferme le manque de mesure mais doit être reproduite ou contrôlée par le gate PERFORMANCE indépendant sur les nouvelles empreintes.

Décision DEV G2 finale 2026-08-20 : la re-REVIEW et la re-SECURITY ont détecté deux chemins adjacents dans le déplacement unitaire. La salle cible d'une exception de cellule est désormais contrôlée par `resourceAllowed` dans la commande et dans la validation canonique des `cellOverrides`; un identifiant cible hors `entityScopes.resource` répond `404` sans changer version ni exceptions. Le cœur de commande refuse aussi explicitement toute réservation `completed`. Les nouveaux négatifs couvrent source autorisée vers cible masquée et déplacement après réalisation. Ce diff invalide les verdicts antérieurs et doit repasser REVIEW, QA et SECURITY/PERFORMANCE.

Décision Gate G2 2026-08-20 : le candidat figé `server.js 5434fb65…` / `app.js 76901020…` est **APPROVED**. REVIEW, QA, SECURITY et PERFORMANCE concluent chacun à 0 P0/P1 sur la même empreinte ; API 29/29, Planning 13/13, Devis 47/47 et suite complète 216/216 sont verts. Le smoke navigateur isolé a validé connexion, affichage du Planning, création d'une réservation confirmée avec Projet et salle, persistance après rechargement, filtres Projet/Statut et absence d'erreur console. La virtualisation reste bornée et interactive (17 × 14 éléments initiaux, déplacements observés à 83/98 ms). Le serveur et le fichier temporaire du smoke ont été arrêtés/supprimés. La RELEASE reste un gate distinct.

Décision retour DEV scrolling G2 2026-08-21 : le contrôle produit a révélé un défilement peu fiable malgré les mesures précédentes. La molette au-dessus de la colonne Ressources faisait défiler la page au lieu de la matrice, la restauration asynchrone de la position provoquait un saut lors du renouvellement de la fenêtre virtualisée et la barre horizontale était trop discrète. Le frontend transmet désormais les deux axes depuis toute la matrice, restaure immédiatement la position avant/après rendu et expose des barres natives plus lisibles. Ce changement invalide les anciens verdicts frontend G2 jusqu'aux revalidations REVIEW, QA et PERFORMANCE impactées.

Preuve DEV scrolling G2 2026-08-21 : candidat frontend `app.js ccf24edfa0335db6…`, CSS `2a71e80473093235…`, test Planning `a71a4301162ce6fb…`, Node `v26.6.0`. Syntaxe, lint, build et diff-check passent ; Planning 14/14 et suite complète 217/217. La colonne Ressources utilise désormais un scroll natif synchronisé avec la grille et conserve l'inertie du trackpad ; aucun gestionnaire de molette artificiel ne s'interpose. La fenêtre garde seize lignes de tampon. Sur le jeu 250 ressources / 10 000 réservations, vingt crans successifs de 120 px conservent la fenêtre `0–41`; un seul renouvellement différé intervient ensuite (`rowStart 0→18`). Ressources, jours et cellules restent alignés. Le lot DEV est prêt pour les revalidations indépendantes impactées.

Validation produit scrolling G2 2026-08-21 : le PO confirme que le défilement vertical corrigé est satisfaisant sur `g2-scroll-native-1`. Les revalidations indépendantes REVIEW, QA et SECURITY/PERFORMANCE sont relancées sur les empreintes exactes avant toute poursuite du sprint suivant.

Décision Gate G2 scrolling 2026-08-21 : le candidat `app.js ccf24edfa0335db6…` / `planning.css 2a71e80473093235…` est **APPROVED**. REVIEW, QA, SECURITY et PERFORMANCE concluent chacun à 0 P0/P1 sur les mêmes empreintes ; Planning 14/14, suite complète 217/217, lint/build/diff-check PASS. Le PO a validé le ressenti du scroll natif. La preuve 250 ressources / 10 000 réservations confirme vingt gestes verticaux sans reconstruction, une synchronisation bidirectionnelle, un seul renouvellement différé après un long parcours et un déplacement horizontal de 1 350 px en 147 ms. G2 est de nouveau franchi.

Décision SPEC Sprint 3 2026-08-21 : G2 étant franchi et confirmé par le PO, le Sprint 3 « Interactions Planning » est lancé depuis les seize lignes exactes du Backlog V1 : US-025 à US-032, US-035 à US-037 et US-043 à US-047. La spécification `docs/specifications/sprint-3-planning-interactions.md` fixe cinq vues dont 3 mois, centre temporel, création/ghost, déplacement/resize, week-end, jours fériés, granularités et snapping. Le premier incrément est S3-A Vues et navigation ; G3 reste bloqué jusqu'au parcours manipulable complet et aux gates indépendants.

Décision DEV Sprint 3 S3-A 2026-08-21 : les vues Jour, Semaine, 6 semaines, Mois et 3 mois sont disponibles dans les deux modes d'affichage. Les plages sont civiles et déterministes, la navigation avance respectivement de 1 jour, 7 jours, 42 jours, 1 mois ou 3 mois, et la date de référence reste recentrée dans la fenêtre virtuelle. La vue 3 mois rend 92 jours pour juillet–septembre 2026 sans condensation du modèle métier. Preuves : frontend `0d47eff71f96484b…`, CSS `00d6625b0947916f…`, tests Planning `592573ca1f071390…`; Planning 16/16, suite complète 219/219, lint/build/diff-check PASS. Smoke navigateur : cinq boutons présents, 92 colonnes logiques en vue 3 mois, ancre `2026-08-17` visible au centre, vue 6 semaines conservée en plein écran, aucune erreur console. Le scrolling natif G2 est préservé. S3-A reste un candidat DEV à faire revoir avec le reste du périmètre G3.

Décision DEV Sprint 3 S3-B 2026-08-21 : le clic-glisser horizontal sur les cellules vides d'une même ressource calcule une période civile dans les deux directions, affiche un ghost continu avec libellé et durée, puis ouvre le formulaire avec début, fin et ressource préremplis. Le ghost est un état DOM temporaire et n'appelle aucune API ; un clic simple conserve son comportement de cible de collage et le bouton `＋` reste l'alternative clavier pour une journée. Annulation du pointeur efface le ghost. Preuves : frontend `43bf8778cf1be6a4…`, CSS `9023c5f04113595a…`, tests Planning `f5c3c35450249327…`; Planning 18/18 et suite complète 221/221, syntaxe/diff-check PASS. Le geste CUA automatisé n'a pas reproduit le drag malgré un ciblage correct des cellules ; le contrôle manuel produit et les gates G3 restent requis avant approbation.

Décision DEV Sprint 3 S3-C 2026-08-21 : le déplacement d'une cellule quotidienne conserve l'identité source et ne déplace aucune autre journée ; le déplacement complet reste volontairement déclenché par `Maj+glisser`. Les bords de redimensionnement sont invisibles au repos, accessibles au clavier et révélés au survol/focus sans flèche persistante. Déplacement complet et redimensionnement affichent désormais un ghost continu avec nature et durée de l'opération. Après dépose, l'interface applique un aperçu optimiste ; un succès remplace cet état par la réponse canonique, tandis qu'un conflit, une version obsolète ou une indisponibilité restaure immédiatement le snapshot local et l'annonce explicitement. Preuves : frontend `db93a6dbffcdb44c…`, CSS `c041ed3776835023…`, tests Planning `cd0e175efb07f02f…`; Planning 20/20, suite complète 223/223, syntaxe/diff-check PASS sous Node v26.6.0. Smoke navigateur : 4 événements manipulables, 8 bords accessibles sans icône visible et rendu Planning sans régression visuelle. Le geste natif de glisser-déposer reste à confirmer manuellement par le PO et pendant les gates G3.

Décision DEV Sprint 3 S3-D 2026-08-21 : le calendrier métier est intégré sans dépendance distante. Les week-ends peuvent être affichés ou masqués explicitement ; les jours fériés français sont calculés de manière déterministe et distingués sur toute la verticale avec libellé accessible. La précision `heure 30/60 min`, `demi-journée 240 min` ou `journée 1 440 min` est persistée avec la réservation et contrôlée côté serveur ; toute combinaison incompatible ou tout calendrier non résolu pour le site répond `422`. Le formulaire et l'OpenAPI exposent les mêmes champs `timeGranularity`, `snapMinutes` et `holidayCalendarId`. Preuves fraîches : serveur `473a94c9c58b0aec…`, frontend `c5abdac3cf039662…`, CSS `e9713e1c83dd2c6a…`, OpenAPI `20febead3f834e34…`, API `a8263bb1edb5eaa1…`, Planning `f89426c7071037a4…`; API 29/29, Planning 22/22 et suite complète 225/225, lint/build/diff-check PASS sous Node v26.6.0. Contrôle navigateur : commandes calendrier/granularité présentes, `14/07 — Fête nationale` rendu dans l'en-tête et les cellules avec infobulle/aria, masquage week-end explicite. Le candidat DEV Sprint 3 est complet et doit maintenant passer les gates indépendants G3 puis le parcours E2E produit.

Décision Gates G3 2026-08-21 : le premier candidat Sprint 3 est **BLOQUÉ — retour DEV** malgré API 29/29, Planning 22/22 et suite complète 225/225 vertes. REVIEW conclut `CHANGES REQUIRED` avec trois P1 : vraie grille horaire absente en vue Jour, alignement réel des instants au snapping non validé par le serveur, déplacement/resize sans alternative clavier. QA confirme les deux P1 UI par contrôle navigateur. SECURITY ajoute un P1 : la commande de déplacement d'une cellule reçoit une clé d'idempotence mais ne la mémorise pas, donc un retry aboutit en conflit de version. PERFORMANCE mesure l'API 250/10k conforme (`read p95 96,39 ms`, conflit `132,47 ms`, écriture `182,93 ms`) mais reste `NOT APPROVED` faute de preuve UI G3 fraîche sur les cinq vues/ghost/resize sous 2 secondes. Condition de reprise : corriger ces quatre défauts produit/techniques, produire la preuve UI Performance, puis rejouer REVIEW, QA et SECURITY/PERFORMANCE sur les nouvelles empreintes.

Décision DEV correctif G3 2026-08-21 : les quatre P1 sont corrigés sur le candidat `server.js faef9ad5…`, `app.js cdf88fc6…`, `planning.css 1928f4ca…`, OpenAPI `a70a98f7…`. La vue Jour/Heure rend désormais 48 créneaux de 30 minutes et positionne les réservations selon leurs heures réelles ; le serveur vérifie l'alignement effectif des instants sur le snapping et conserve la compatibilité des données antérieures. Déplacement et redimensionnement disposent d'alternatives clavier explicites, avec focus visible. La commande de déplacement d'une cellule mémorise sa clé d'idempotence : un rejeu identique restitue le résultat sans second audit/SSE et un payload divergent répond `409`. Preuves fraîches Node v26.6.0 : API 30/30, Planning 24/24, Devis 47/47, suite complète 228/228, lint/build/diff-check PASS. Contrôle navigateur isolé : vue Jour `48` colonnes et `1 148` cellules DOM virtualisées ; les cinq vues se rendent en `1 253 / 785 / 790 / 788 / 783 ms`, sans erreur capturée. Le candidat retourne aux gates indépendants ; aucun statut G3 `APPROVED` n'est revendiqué avant leurs verdicts.

Décision DEV correctif G3 — seconde passe 2026-08-21 : la re-REVIEW a fermé la grille horaire et le snapping serveur, puis détecté trois consommateurs frontend. Le nouveau candidat `app.js ca2471b6…`, tests Planning `bb969368…` supprime tout offset fixe : les instants sont construits et relus avec le fuseau IANA du site (`Europe/Paris`, été et hiver). Une réservation historique sans `timePolicyVersion` reste éditable sans recevoir implicitement `timeGranularity`/`snapMinutes`; seule une création ou une modification explicite de précision active la politique Sprint 3. Après une commande clavier, le focus est restauré sur la réservation ou la poignée équivalente après le rendu asynchrone. Preuves fraîches : Planning 26/26, API 30/30, Devis 47/47, suite complète 230/230, lint/build/diff-check PASS. Le contrôle navigateur confirme toujours la grille Jour/Heure ; l'outil navigateur n'a pas pu adresser de façon stable un article virtualisé pour rejouer la séquence de flèches, limite transférée à la re-REVIEW/E2E. Tous les gates impactés doivent porter sur cette nouvelle empreinte frontend.

Décision DEV correctif G3 — troisième passe 2026-08-21 : les deux derniers P1 de re-REVIEW et le P1 Performance sont corrigés sur le candidat `server.js faef9ad5…`, `app.js 7dada2a0…`, `planning.css 1928f4ca…`, tests Planning `d450d34c…`, OpenAPI `a70a98f7…`. La grille Jour/Heure dérive désormais ses créneaux des instants réels du fuseau IANA : `46` demi-heures le 29 mars 2026, `50` le 25 octobre 2026, avec occurrence explicite de l'heure répétée ; une heure locale inexistante est refusée et un instant ambigu existant est conservé sans réécriture. Les réservations quotidiennes multi-jours restent bornées à leur cellule 09:00–18:00. Lorsqu'une commande clavier déplace l'objet hors de la fenêtre, la date d'ancrage est recentrée et le focus revient sur l'objet ; la région Planning sert de fallback accessible. Le cache des formateurs IANA supprime le surcoût par réservation. Preuves fraîches Node v26.6.0 : Planning 28/28, API 30/30, suite complète 232/232, lint/build/diff-check PASS ; benchmark HTTP 250 ressources/10 000 réservations conforme (`lecture p95 104,48 ms`, conflit `144,55 ms`, écriture `179,18 ms`, rejeu `155,70 ms`). Le candidat retourne aux gates indépendants sur ces empreintes exactes.

Décision DEV correctif G3 — quatrième passe 2026-08-21 : la re-REVIEW V3 a validé les transitions DST du rendu mais identifié deux gestes clavier adjacents. Le candidat `app.js 9cb7d996…`, tests Planning `a9a98223…` déplace et redimensionne désormais par addition d'instants réels de 30 minutes, et non par reconstruction d'une heure locale ambiguë : le passage printemps saute correctement de 01:30 à 03:00 et l'heure d'automne parcourt les deux occurrences sans saut de 90 minutes. Un redimensionnement qui place sa poignée hors de la journée visible recentre l'ancre sur la nouvelle date de début ou de fin avant de restaurer le focus. Preuves fraîches : Planning 29/29, suite complète 233/233, lint/build/diff-check PASS ; benchmark HTTP 250/10 000 conforme (`lecture p95 100,07 ms`, conflit `136,92 ms`, écriture `174,16 ms`, rejeu `151,31 ms`). Les gates REVIEW, QA et SECURITY/PERFORMANCE doivent être rejoués sur cette dernière empreinte frontend.

Décision Gates G3 V4 2026-08-21 : **APPROVED — 0 P0 / 0 P1** sur le candidat figé `server.js faef9ad5…`, `app.js 9cb7d996…`, `planning.css 1928f4ca…`, tests Planning `a9a98223…`, API `165a1299…`, OpenAPI `a70a98f7…`. REVIEW, QA, SECURITY et PERFORMANCE ont rendu quatre verdicts indépendants APPROVED. Preuves terminales : Planning 29/29, API 30/30, Devis 47/47, suite complète 233/233, lint/build/diff-check PASS ; benchmark indépendant 250 ressources/10 000 réservations (`lecture p95 100,36 ms`, conflit `136,47 ms`, écriture `173,83 ms`, rejeu `154,73 ms`). Smoke navigateur local : connexion réussie, vue Jour/Heure normale `48` créneaux (`3 456 px`), 29 mars 2026 `46` créneaux (`3 312 px`), 25 octobre 2026 `50` créneaux (`3 600 px`), région Planning accessible et aucune alerte. La reproduction physique exhaustive move/resize avec persistance après redémarrage reste au gate E2E/Integration aval ; elle ne remet pas en cause le verdict technique G3.

Décision DEV issue du gate E2E G3 2026-08-21 : le parcours navigateur sur données temporaires a créé `E2E G3 — Session test`, l'a déplacée au clavier, étendue de 1 à 4 jours, annulée, puis relue annulée après redémarrage. Il a révélé qu'un rafraîchissement SSE tardif retirait le focus de la poignée environ une seconde après sa première restauration. Le candidat `app.js 35826e96…`, tests Planning `833ece1c…` conserve pendant 2,5 secondes une intention de focus bornée et la rejoue après les rendus SSE ; il ne vole pas le focus si l'utilisateur a déjà quitté la matrice. Preuve navigateur après correction : focus toujours sur `Étirer la fin de E2E G3 — Session test` après 1,2 seconde ; profil lecteur sans bouton de réservation, sans cellule créable et sans événement mutable ; persistance de quatre cellules annulées après redémarrage. Suite complète 233/233, lint/build/diff-check PASS. Les approbations G3 antérieures ne couvrent pas ce diff frontend : re-REVIEW, re-QA et revalidation SECURITY/PERFORMANCE sont en cours.

Décision terminale Integration/E2E G3 2026-08-21 : **APPROVED — 0 P0 / 0 P1** sur `server.js faef9ad5…`, `app.js 35826e96…`, `planning.css 1928f4ca…`, tests Planning `833ece1c…`. Le parcours navigateur réel couvre connexion administrateur, création projet/ressource seedée et réservation, déplacement clavier 17→18 août, redimensionnement 1→4 jours, maintien du focus après rafraîchissement SSE, annulation logique, arrêt/redémarrage du serveur sur le même fichier, relecture des quatre cellules en statut annulé, puis connexion lecteur sans commande de création ni événement mutable. Le serveur temporaire 8207 a été arrêté proprement et aucune donnée utilisateur n'a été touchée. Après le correctif E2E, REVIEW, QA, SECURITY et PERFORMANCE ont tous revalidé le nouveau hash à 0 P0/P1 ; Planning 29/29, API 30/30, Devis 47/47, suite complète 233/233, lint/build/diff-check PASS. G3 et ses gates Integration/E2E sont franchis ; la suite produit est le cadrage/lancement du Sprint 4 selon le Backlog V1, avant toute RELEASE globale.

Décision DEV correctif G5 2026-08-21 : les six P1 REVIEW/SECURITY sont corrigés. Une option simple sans groupe se confirme sans arbitrer les autres ; une double option groupée conserve les perdants et expose des alternatives structurées. Le SSE possède maintenant une matrice de permissions explicite et fail-closed, une connexion maximale par session et une capacité globale. Les snapshots Personnel respectent le site de l'indisponibilité, y compris dans PlanyBot. Les gestes clavier acquièrent le même verrou de présence que la souris ; la libération manuelle, le logout et le TTL diffusent `reservation.presenceReleased.v1`. Preuves DEV fraîches : API 39/39, Planning 43/43, temps réel 1/1, Devis 47/47, suite complète 259/259, lint/build/diff-check PASS. Le candidat retourne aux gates REVIEW, QA et SECURITY/PERFORMANCE ; aucun verdict G5 APPROVED n'est revendiqué par l'auteur.

Décision terminale Gate G5 2026-08-21 : **APPROVED — 0 P0 / 0 P1** sur le candidat `server.js b9b6294f…`, `app.js 04f7a5a9…`, `planning.css 4016e6d8…`, tests API `1e581cec…`, Planning `9c5721e0…` et temps réel `d8b17b3…`. Le dernier défaut inter-site est fermé : une indisponibilité Boulogne n'est ni listée ni annulable par un planificateur limité à Paris, y compris au rejeu idempotent. REVIEW, QA, SECURITY et PERFORMANCE ont rendu quatre verdicts indépendants APPROVED. Preuves finales : API 40/40, Planning 43/43, temps réel 1/1, migration 1/1, suite complète 260/260, lint/build/diff-check PASS. Benchmark représentatif 250 ressources / 10 000 réservations : lecture p95 116,60 ms, conflit 166,27 ms, écriture 206,25 ms et batch100 249,66 ms. Les limites P2 non bloquantes restent documentées dans les rapports (charge SSE extrême/UI authentifiée, faible marge batch100, défense en profondeur du quota de présences). G5 est franchi ; Integration/E2E et RELEASE globale restent des gates distincts.

Décision Integration/E2E G5 2026-08-21 : **APPROVED — 0 P0 / 0 P1** sur les mêmes empreintes. Un serveur isolé a été démarré sur le port 8210 avec un fichier temporaire dédié. Dans le navigateur, l'administrateur s'est connecté, a déplacé au clavier l'option `Conformation` du 17 au 18 août ; le verrou de présence a été acquis/libéré par le parcours, le Planning a annoncé `Synchronisé` et `Réservation entière déplacée`. La nouvelle date est restée visible après rechargement/reconnexion, puis après arrêt et redémarrage complet du serveur sur le même fichier. Le profil lecteur a ensuite été contrôlé : aucun bouton de réservation, aucune cellule créable et aucun événement mutable. Aucune erreur console n'a été observée. Le serveur, l'onglet de test et tous les fichiers temporaires `planify-g5-e2e*` ont été supprimés proprement. La RELEASE globale reste le prochain gate distinct.

Audit préalable RELEASE V1 2026-08-21 : **BLOQUÉ — figement Git requis**. Le candidat G5 est techniquement vert et reproductible, mais `git status --short` retourne l'intégralité du dépôt en fichiers non suivis et aucun commit de référence n'existe sur `main`. Il est donc impossible de démontrer que tous les rapports portent sur un état versionné immuable, de produire un diff de release fiable ou d'associer sans ambiguïté une version/tag aux empreintes approuvées. Aucun numéro de version, tag ni commit n'a été créé implicitement. Condition de déblocage : autorisation du PO pour figer le dépôt dans un premier commit de référence, puis mise à jour cohérente de `package.json`, `CHANGELOG.md` et `README.md`, nouvelle suite complète/démarrage final et relecture du diff de release.

Décision RELEASE V1 2026-08-21 : le PO a autorisé le premier figement Git et la préparation de la candidate **`0.2.0-rc1`**. `package.json`, `CHANGELOG.md` et `README.md` sont alignés ; `data/*.json`, `output/` et `tmp/` sont exclus pour protéger données de travail, exports client et artefacts temporaires. Preuves finales Node v26.6.0 : `npm test` 260/260, `npm run lint` PASS, `npm run build` PASS et `git diff --check` PASS. Le démarrage isolé sur le port 8211 retourne HTTP 200 sur `/` et 401 attendu sur `/api/v1/auth/me` sans session ; le serveur et ses fichiers temporaires ont été supprimés. Le premier commit local constitue la baseline reproductible de la candidate. Aucun déploiement distant ni publication externe n'est réalisé par ce gate local.

Décision produit 2026-08-14 : l’ordre de livraison est désormais strictement `01 Organisation → 02 Ressources → 04 Projets → 03 Planning`, puis modules aval. Une réservation exige un projet/émission référencé. Stock 07a reste techniquement intégré mais son gate produit est différé jusqu’à validation des fondations.

| Module | État | Responsable | Tests | Dépendances / blocages |
|---|---|---|---|---|
| Sprint 0 V1 / Gate G0 | **APPROVED — G0 franchi** | Lead Developer / Intégration | Candidat du 2026-08-19 : `server.js` SHA-256 `ae82955e…`, `app.js` `ad22b4fa…`, `planning.css` `7aadb6a0…`, fondations `39ff84e1…`. REVIEW, QA, SECURITY et PERFORMANCE finaux : APPROVED, 0 P0/P1. Lint/build/diff-check PASS ; fondations 15/15, API 27/27, Planning 10/10, suite complète 195/195. Parcours navigateur déterministe validé : connexion, création Client/Projet/Ressource, réservation avec projet obligatoire, filtre Projet+Ressource, cellule visible et persistance après rechargement. Deux défauts E2E ont été corrigés puis revalidés : création Ressource sans ancien `prompt()` avec idempotence, et formulaire long/barre latérale utilisables à hauteur laptop. | Sprint 1 peut démarrer conformément à l'Ordre/Backlog V1. La RELEASE reste bloquée tant que l'état entièrement non suivi n'est pas figé proprement dans Git et que les gates propres au Sprint 1 ne sont pas franchis. |
| Sprint 1 V1 / Gate G1 | **APPROVED — G1 franchi** | Lead Developer / Intégration | Candidat du 2026-08-20 : serveur `32681574…`, frontend `ebd7ab42…`, OpenAPI `8a36107f…`, tests Sprint 1 `f5df2d98…`, API `5265b7a3…`. REVIEW, QA, SECURITY et PERFORMANCE : APPROVED, 0 P0/P1. Preuves fraîches : Sprint 1 15/15, API 27/27, fondations 15/15, suite complète 210/210 ; lint/build/OpenAPI/diff-check PASS. Recherche p95 0,347 ms ; analytics p95 12,243 ms. E2E navigateur : création Client/Projet/unité/prestation, recherche Projet et prestation, dashboard analytique, persistance après redémarrage. | Aucun blocage G1. P2 suivis : précisions OpenAPI tarifs, a11y avancée de la recherche, baseline Git à figer. Sprint 2 peut démarrer ; RELEASE non engagée. |
| Sprint 2 V1 / Gate G2 | **APPROVED — G2 franchi** | Lead Developer / Intégration | Candidat du 2026-08-21 : frontend `ccf24edfa0335db6…`, CSS `2a71e80473093235…`, Planning 14/14, suite complète 217/217, lint/build/diff-check PASS. REVIEW, QA, SECURITY et PERFORMANCE : APPROVED, 0 P0/P1. Browser 250/10k : scroll natif Ressources, vingt gestes sans reconstruction, renouvellement différé `rowStart 0→18`, axes et alignement conservés. | Aucun blocage G2. P2 suivis : smoke multi-navigateur et enrichissement a11y de la région scrollable. Le prochain sprint peut démarrer ; RELEASE reste distincte. |
| Sprint 3 V1 / Gate G3 | **APPROVED — INTEGRATION/E2E FRANCHIS** | Lead Developer / REVIEW / QA / SECURITY / PERFORMANCE / Intégration | Candidat : serveur `faef9ad5…`, frontend `35826e96…`, CSS `1928f4ca…`, tests Planning `833ece1c…`; Planning 29/29, API 30/30, Devis 47/47, suite 233/233. E2E navigateur : create, move, resize, focus SSE, cancel, restart/persistance, viewer. | Aucun P0/P1. Prochaine étape : Sprint 4 selon l'ordre et le Backlog V1 ; RELEASE globale reste hors de ce lot. |
| Sprint 4 V1 / Gate G4 | **APPROVED — INTEGRATION/E2E FRANCHIS** | Lead Developer / REVIEW / QA / SECURITY / PERFORMANCE / Intégration | Candidat serveur `31f2e713…`, app `16dc6c21…`, API `05a7d439…`, Planning `7cb607aa…`; REVIEW/QA/SECURITY/PERFORMANCE à 0 P0/P1, suite 245/245. Batch100 p95 `220,15 ms`. E2E isolé : sélection rectangulaire 5 cellules, synchronisation, rechargement/reconnexion et 5 réservations persistées ; parcours commercial antérieur `5 → +3 → 0` conservé. | Aucun blocage G4. P2 : réponses d'erreur OpenAPI et audit batch récapitulatif. Sprint suivant autorisé ; RELEASE globale reste distincte. |
| Sprint 5 V1 / Gate G5 | **APPROVED — INTEGRATION/E2E FRANCHIS** | Lead Developer / REVIEW / QA / SECURITY / PERFORMANCE / Intégration | Candidat final : serveur `b9b6294f…`, frontend `04f7a5a9…`, API `1e581cec…`, Planning `9c5721e0…`, temps réel `d8b17b3…`. REVIEW/QA/SECURITY/PERFORMANCE : APPROVED, 0 P0/P1. API 40/40, Planning 43/43, temps réel 1/1, migration 1/1, suite 260/260. E2E : login admin, option déplacée au clavier avec présence, synchronisation, reload/relogin, restart/persistance, puis lecteur sans mutation. | Aucun blocage G5. P2 suivis : charge SSE/UI extrême non rejouée, marge batch100 étroite et quota de présences en défense en profondeur. Prochaine étape : gate RELEASE globale. |
| Sprint 8 V1 / Gate G8 | **APPROVED — INTEGRATION/E2E FRANCHIS** | Lead Developer / REVIEW / QA / SECURITY / PERFORMANCE / Intégration | Candidat applicatif `68489b1…`; quatre gates indépendants à 0 P0/P1. Foundations 16/16, ciblés QA 106/106, suite 339/339, lint/build/diff-check PASS. E2E : six dashboards, filtre URL, drill-down, export, lecteur sans Finance, cycle startup/login/logout/401/reconnexion sur 24 routes composées. | Aucun P0/P1. P2 suivis dans les rapports : accessibilité avancée, matérialisation avant plafond export et profilage navigateur fin. |
| Architecture & contrats | Terminé | Agent 01 | Revue documentaire | Aucun |
| Design system & parcours | Terminé | Agent 02 | Revue UX documentaire | Validation produit à venir |
| Core / référentiels | Intégré | Agent 03 | Domaine + API | Persistance JSON locale, SQLite reporté |
| Planning engine | Incrément Post-production intégré, revue indépendante requise | Intégration | Syntaxe/diff-check verts ; smoke navigateur géométrique Semaine/Mois/6 semaines le 2026-08-16 | Sélecteur Post-production/Location, matrice salle × jour. En-tête, cellules et lignes Ressources utilisent désormais une géométrie commune : aucune dérive après une ligne multi-événements et aucune colonne fantôme en plein écran. Les week-ends sont plus sombres et restent alignés verticalement ; les dates Mois/6 semaines sont compactes et lisibles. La vue Semaine charge les semaines adjacentes et reste sélectionnée en plein écran ; la barre supérieure est synchronisée et accepte glisser, molette, Maj+molette et clavier. Sortie par bouton ou Échap. Le déplacement vertical utilise une commande API dédiée à la cellule source. Planning Location volontairement différé au prochain incrément |
| Frontend | En cours, validation E2E à faire | Agent intégrateur | Test domaine à rejouer ; smoke navigateur à faire | Démarrage corrigé : toute ouverture affiche la connexion, sans restauration implicite d’un utilisateur prototype ou d’une session existante. Une réponse API 401 hors connexion ramène désormais à la connexion avec message explicite. La suite API complète reste à rejouer hors environnement restreint, qui interdit l’ouverture d’un port local. |
| Clients 05 / contacts / tarifs dédiés | Nouveau candidat DEV, revalidation indépendante requise | DEV Clients 05 + Intégration | 2026-08-18 : `node --test tests/clients.test.js` 11/11 et `npm test` 164/164 ; syntaxe serveur/tests verte | Compte client professionnel, contacts 0..n, suppression logique et grilles tarifaires privées. L’import Excel accepte le tableau normalisé et les blocs `Stockage MAM`, `Postproduction`, `Logistique`; chaque montant Heure/Jour/Semaine/Mois/Forfait devient un tarif distinct, les cellules vides ou non chiffrées ne créent rien et la colonne Remise est ignorée. Le parseur préserve les cellules Excel vides et arrondit les artefacts décimaux du classeur BUBBLE. La prévisualisation reste obligatoire avant confirmation. La fiche client propose `Voir les tarifs`, ouvrant une page de consultation filtrable qui n’expose que les prix de vente actifs du tenant courant. Les nouveaux devis figent le contact et résolvent les tarifs dans l’ordre `projet > client > catalogue`, puis sélectionnent le tarif correspondant à l’unité demandée. Une salle individuelle `Salle de montage AVID N` hérite désormais du tarif générique client/projet `Salle de montage Avid` (95 EUR/jour pour BUBBLE) ; un tarif direct de même portée reste prioritaire. Re-REVIEW, QA et Security restent nécessaires avant RELEASE. |
| Code review | Approved | Agent 07 | Revue finale effectuée | Aucun P1 ouvert |
| QA | Approved | Agent 08 | 32/32 domaine + API/intégration/sécurité | Aucun |
| E2E utilisateur | Smoke validé | Agent 09 | Connexion, dashboard, planning API | Automatisation UI exhaustive post-RC |
| Performance | Approved | Agent 10 | 100 ressources / 10 000 réservations | Mono-processus local |
| Sécurité | Approved | Agent 11 | Auth, CSRF, RBAC, isolation, statique, fail-closed | Aucun bloquant connu |
| Intégration | Intégré RC1 | Agent 12 | Frontend/API local vérifié | Aucun |
| Release V1 | **0.5.0-rc4 APPROVED — candidate locale** | Intégration / PO | G0–G8, Integration/E2E et recettes visuelles approuvés ; empilement Projet, dimensions de scroll, DOM dense et index Planning corrigés. Preuves : 341/341, lint/build/diff-check PASS, 5 actifs runtime. | Aucun P0/P1. Publication ou déploiement distant non demandé ; limites P2 conservées dans les rapports. |
| Organisation 01 / Fiscalité 01b | DEV intégré, revalidation indépendante requise | Product/Domain Organisation + Backend/Frontend 01 | 84/84 tests complets le 2026-08-15 ; syntaxe et diff-check propres ; dernier benchmark HTTP p95 1,574 ms lecture / 3,214 ms écriture | Exemples de saisie ajoutés ; archivage logique Organisation et désactivation Service/Ressource réservés aux administrateurs, via une commande compacte « − » et un avertissement explicite avant confirmation, avec contrôles de dépendances et conservation de l'historique. Les anciens verdicts APPROVED doivent être rejoués sur ce nouveau candidat avant Ressources 02 ; validation produit O2/O3 reste à clôturer |
| Ressources 02 | DEV partiel intégré, revalidation indépendante requise | Product/Domain Ressources + Intégration | 90/90 tests complets le 2026-08-15 ; smoke navigateur Ressources et Parc matériel | Premier incrément visible : liste professionnelle des salles et affectation/dépose d’exemplaires Stock par site. La fondation Organisation reste le prérequis de validation avant poursuite Ressources 02 |
| Projets 04 + Planning 03 | DEV initial Post-production intégré, revalidation indépendante requise | Product Planning/Workflow + Intégration | Gate SPEC APPROVED ; 97/97 tests complets ; smoke navigateur semaine/mois/sélecteur | Projet référencé obligatoire. Première tranche limitée à la matrice Post-production ; exceptions unitaires de cellule intégrées, Planning Location reste hors de ce candidat |
| Architecture cible v1 | Approved | Agent 01 + Review 07 | Review SPEC 0.2 approuvée | Synoptique PO adopté, roadmap phases 0–8 |
| Matériel / Location 06a | Spec approved, pending 07a | Product/Domain Rental | Plan QA 0.2 prêt | Dépend du socle Stock 07a |
| Stock / Logistique 07a | Nouveau candidat DEV, revalidation indépendante requise | Backend Stock 07a + Frontend 05 + Intégration | 6/6 ciblés Assistant catalogue ; 90/90 complets ; syntaxe/diff-check propres ; smoke navigateur Lenovo avec photo et préremplissage | Assistant 2026.08-local.2 : recherche sémantique légère, catalogue caméra/informatique/serveur/stockage/licences, photos fabricant locales sourcées, suivi sérialisé ou par quantité, coûts et installation en salle. Les anciens verdicts REVIEW/QA/SECURITY/PERFORMANCE ne couvrent pas ce nouveau diff |
| Projet 04 + Commercial 08 / Budgets & Devis | Nouveau candidat DEV conforme au synoptique PO — REVIEW indépendante requise | Commercial 08 + Intégration | 45/45 tests Devis ciblés ; `npm test` 167/167 ; syntaxe serveur/frontend verte le 2026-08-18 | Parcours canonique : Client actif → Projet → soit Budget confirmé par le client puis conversion manuelle, soit Devis direct manuel, depuis un Planning existant ou depuis un planning client analysé par PlanyBot. Les tarifs sont résolus automatiquement `projet > client > catalogue`. L’import PlanyBot vers un Devis brouillon ajoute uniquement les lignes sélectionnées et ne crée aucune réservation. L’acceptation du Devis exige `quote.accept`, fige la version commerciale/fiscale de référence et crée une reconnaissance de CA traçable ; un successeur remplace explicitement la reconnaissance active sans réécrire l’historique. Le portail Client externe reste hors de cet incrément local : la confirmation est enregistrée par un rôle interne autorisé. |
| Intégration Devis → Projet → Planning | DEV étendu — nouveau candidat pour REVIEW indépendante | Commercial 08 + Planning 03 | Devis ciblés 47/47 ; Planning Post-prod 10/10 ; `npm test` 170/170 ; syntaxe backend/frontend verte ; contrôle navigateur réussi le 2026-08-18 | Un Devis accepté expose `Planifier le devis` et ouvre le Planning de son Projet sans créer automatiquement de réservation. L'opérateur affecte chaque cellule à une prestation vendue ; PlanyBot recalcule vendu/planifié/reste/dépassement après toute création, duplication, redimensionnement, déplacement ou annulation. Un unique Devis complémentaire brouillon est créé et synchronisé automatiquement pour les excédents, avec tarif `projet > client > catalogue`; le Devis accepté et son snapshot fiscal HT/TVA/TTC restent intacts. |
| Planning cible Post-production | DEV étendu — nouveau candidat pour REVIEW indépendante | Planning 03 + Intégration | Planning Post-prod 10/10 ; Devis ciblés 47/47 ; `npm test` 170/170 ; syntaxe backend/frontend verte ; smoke navigateur cellule source → date/salle cible puis annulation réussi le 2026-08-18 | Projet obligatoire ; Jour/Semaine/Mois/6 semaines ; multi-périodes ; déplacement normal d'une seule cellule sans suppression d'une autre réservation. La sélection est désormais portée par la cellule exacte et ne surligne plus toute une série. Le copier-coller duplique uniquement les journées sélectionnées, de manière idempotente et relative à la date/salle cible. Le redimensionnement se fait depuis des bords invisibles au repos avec curseur horizontal, sans flèches persistantes. Le smoke a confirmé une unique sélection, une copie active sur la seule cellule cible et sa suppression logique par Annuler, sans autre réservation active ajoutée. Le déplacement complet reste réservé à Maj+glisser ; week-ends, annuler/rétablir et repère cible sont conservés. Tous les chemins gardent projet/devis/ligne, contrôlent disponibilité et conflits, auditent avant/après et déclenchent le recalcul PlanyBot. |
| PlanyBot — Assistant Planning | G6 APPROVED — E2E franchi, RELEASE requise | Planning 03 + Intégration | Tous les gates techniques et INTEGRATION approuvés ; E2E navigateur connexion/proposition/refus/confirmation/lecteur/SSE/redémarrage ; ciblés 104/104 ; `npm test` 270/270 ; lint/build/diff-check verts le 2026-08-23 | Copilote local sans SaaS : recherche, recommandation explicable, proposition confirmable et import Excel/CSV/PDF avec clarification humaine versionnée. Aucun acte métier avant confirmation ; réservation unique après confirmation, auditée, actualisée et persistée. Prochaine condition : RELEASE sur le même candidat. P2 suivi : double parcours de lecture XLSX générique. |
| Finance / Rentabilité 09a | **G7 APPROVED — INTEGRATION/E2E FRANCHIS** | Product/Domain Finance + Backend/Frontend / REVIEW / QA / SECURITY / PERFORMANCE / Intégration | Candidat produit `7051fe4`, REVIEW `ab639b5`. S7-D 8/8, API 42/42, ciblés Sécurité 41/41, full 312/312, lint/build/diff-check/OpenAPI PASS. REVIEW, QA, SECURITY et PERFORMANCE : APPROVED, 0 P0/P1. E2E isolé : création coût interne `95,00 €`, dépense Projet `250,00 €`, calcul de marge, reload/reconnexion, redémarrage et persistance ; lecteur refusé avec `finance.read` requis ; console sans erreur. | Aucun P0/P1. Les options perdues sont exclues, l'arbitrage est réalisé après scopes et les dépenses Projet conservent leur prestation. Serveur/onglet/fichiers temporaires arrêtés et supprimés. P2 non bloquants conservés dans les rapports. Étape suivante : consolidation RELEASE. |
| Analytics 10a | Spec approved, pending | Product/Domain Analytics | Plan QA 0.2 prêt | Événements Planning/Finance/Stock |

## Décisions de release

- Les contrats de fondation 0.3 sont approuvés. Chaîne immuable : `foundation-01-organization-v2-to-v3` → `foundation-01b-organization-fiscal-v3` → `foundation-02-resources-v3-to-v4` → `foundation-04-projects-v4-to-v5` → `foundation-03-planning-v5-to-v6`.
- `companyId` est l'identifiant technique unique d'une société ; `organizationId` est refusé. Les anciennes réservations sans projet seront rattachées à un Client/Projet techniques audités avant activation de l'invariant `projectId` obligatoire.
- Organisation fournit un profil fiscal et monétaire multi-territoires. Pour la France, 20 % est proposé comme taux de TVA initial mais reste configurable. Commercial 08 fige à la création d'un Budget/Devis un snapshot société/taux et ses montants HT, TVA et TTC ; une modification ultérieure du profil n'est jamais rétroactive.
- Commercial 08 applique désormais cette fondation de bout en bout : un Budget/Devis peut naître du Projet sans aucune réservation, puis être relié à zéro, une ou plusieurs réservations. Le lien commercial/planning reste optionnel et les montants fiscaux sont calculés exactement en unités mineures sérialisées. Un document sans site est réservé aux rôles à portée Organisation ; les autres acteurs doivent choisir un site autorisé.
- Le synoptique PO du 2026-08-18 fixe deux branches après le Projet. Un Budget doit être confirmé par le client avant sa conversion manuelle en Devis et ne contribue jamais directement au CA. Un Devis peut être créé manuellement, depuis le Planning ou depuis un planning client analysé par PlanyBot. Seul un Devis accepté alimente le CA, à partir de sa version et de son snapshot fiscal immuables ; chaque création, analyse, conversion, acceptation et remplacement est auditée.
- Clients 05 ajoute sans migration de stack les comptes enrichis, contacts versionnés et grilles tarifaires client privées. Le snapshot du contact est figé avec le document/version commerciale. La résolution des tarifs est déterministe : projet, puis client, puis catalogue ; un changement de contact ou de grille ne réécrit jamais un devis existant.
- Le canon Projet/Budget/Devis/Planning du PO (§1–58, reçu le 2026-08-16) est prioritaire. La migration `commercial-08-project-rates-v2` sépare physiquement Budget et Quote, conserve leurs snapshots de révision dans des collections distinctes et ajoute les tarifs locaux sans réécrire les projets historiques. Les prix commerciaux restent indépendants du Planning et du catalogue après application.
- Le Parc matériel dispose d’un assistant catalogue local et versionné : recherche tolérante, visuels locaux et préremplissage contrôlé sans appel SaaS/CDN. Toute donnée proposée reste modifiable et exige une validation humaine. Les exemplaires sérialisés portent série, numéro interne, coûts et devise ; leur installation dans une salle est un transfert physique Stock audité.
- Le Planning Post-production utilise désormais une matrice canonique `salle × jour` dans les trois vues. Une réservation portant sur plusieurs jours est rendue dans chaque cellule de sa période. Chaque cellule conserve une identité source ; son déplacement crée une exception versionnée pour cette seule journée, avec validation de la salle cible et des conflits, sans déplacer le reste de la série. Un mode immersif masque le shell, affiche le mois civil complet ou une fenêtre de 6 semaines, puis revient au mode normal par une commande visible ou la touche Échap. Le Planning Location reste isolé et n'est pas simulé par les données Post-production.
- Le catalogue de démonstration Planning contient 55 salles de montage AVID numérotées `101–111`, `201–211`, `301–311`, `401–411`, `501–511`, ainsi que 20 postes Remote AVID numérotés `1–20`. Les ressources utilisateur supplémentaires sont conservées lors de cette migration de données.
- Le planning ELIOTE Post Prod reprend ces 55 salles AVID et 20 postes Remote. Il ajoute 8 studios Pro Tools de mixage — 3 en 5.1 et 5 en stéréo — ainsi que 3 salles d’étalonnage DaVinci Resolve. La migration additive et rejouable conserve toute ressource ELIOTE créée par un utilisateur.
- Exécution locale autonome, sans dépendance npm externe.
- API et frontend servis par un processus Node unique.
- Persistance atomique JSON retenue pour cette RC afin de conserver le démarrage zéro dépendance ; la cible SQLite décrite dans l’architecture reste un chantier avant release stable.
- Le fallback `localStorage` est réservé au mode prototype lorsque l’API est indisponible.

## Commandes de référence

```bash
npm test
npm start
```

Application : `http://localhost:8080` par défaut.
